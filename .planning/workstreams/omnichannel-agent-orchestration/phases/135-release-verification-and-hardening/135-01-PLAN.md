---
phase: 135-release-verification-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/release-gate.test.ts
  - tests/coverage-pins.test.ts
  - scripts/release-gate.ts
  - package.json
autonomous: true
requirements: [TEST-02]
---

<objective>
Turn the seven TEST-02 areas from suites that happen to exist into a named gate that
fails loudly when any of them regresses or disappears, and pin the action-type coverage
of every safety-critical set so the Xkedule class of gap cannot recur silently.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the coverage of safety-critical sets</name>
  <files>tests/coverage-pins.test.ts</files>
  <action>Phase 133 tested the idempotency guard's behavior thoroughly and never tested which action types reach it, so the Xkedule booking mutations bypassed it entirely. Close that class of gap: for every set that drives a security or safety decision — `SIDE_EFFECTING_ACTIONS`, `COMMERCE_WRITE_ACTIONS`, the partner-edge denial reasons, the channel enum — assert the full membership explicitly, and assert that every mutating action type the Action Engine dispatches is either in the side-effecting set or explicitly listed as a deliberate read. Derive the Action Engine's action types from source rather than retyping them, so a newly added action fails this test until someone classifies it.</action>
  <verify>npx vitest run tests/coverage-pins.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the release gate as a named, deterministic subset</name>
  <files>tests/release-gate.test.ts, scripts/release-gate.ts, package.json</files>
  <action>Declare the gate membership explicitly — one named suite per TEST-02 area plus the coverage pins — as data, not a glob, so removing a suite is a visible edit. Add a test asserting every declared member file exists and that every TEST-02 area maps to at least one member, so deleting a suite fails the gate rather than shrinking it silently. Add a runner script and an npm script that executes the subset, plus `npm run workflows:validate`, and exits non-zero on any failure. Do not run the full suite: 30 files fail for pre-existing live-database and module-resolution reasons, so a gate over everything would be permanently red and therefore ignored.</action>
  <verify>npx vitest run tests/release-gate.test.ts tests/coverage-pins.test.ts</verify>
</task>

</tasks>

<success_criteria>Every TEST-02 area is covered by a declared gate member; removing or regressing a member fails the gate; every mutating Action Engine action type is classified; the gate runs deterministically and exits non-zero on failure.</success_criteria>
