---
phase: 135-release-verification-and-hardening
plan: 03
type: execute
wave: 2
depends_on: [135-01, 135-02]
files_modified:
  - .github/workflows/release-gate.yml
  - docs/agents/uat-checklist.md
  - docs/agents/release-gate.md
  - scripts/release-gate.ts
autonomous: true
requirements: [TEST-04]
---

<objective>
Make the gate block rather than advise, and give a human a checklist they can follow
without reading the codebase.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the gate into CI so it can actually fail a change</name>
  <files>.github/workflows/release-gate.yml, scripts/release-gate.ts, docs/agents/release-gate.md</files>
  <action>None of the fifteen existing workflows runs the test suite; `build-deploy.yml` goes straight from build to deploy. Add a workflow that runs the 135-01 gate subset, the 135-02 latency test, and `npm run workflows:validate` on pull requests and on pushes that touch the orchestration paths. It must exit non-zero and block on failure — a gate that only reports is not a gate. Do NOT modify `build-deploy.yml`: it is the live deploy path, and coupling a new gate to it risks blocking production deploys on an unproven check. Document in `release-gate.md` what the gate covers, what it deliberately does not (the 30-file live-database baseline), and how to add a suite to it. Note the build itself is not in this workflow and say why — it needs an 8 GB heap and several minutes, and `build-deploy.yml` already builds on every push to main.</action>
  <verify>npx vitest run tests/release-gate.test.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Write a UAT checklist a non-author can execute</name>
  <files>docs/agents/uat-checklist.md</files>
  <action>Write the voice and text UAT checklist TEST-04 requires, executable by someone who has not read this repository. Cover both channels reaching the same specialist definition, an explicit-intent request routing directly, an ambiguous request going to the entry orchestrator, a delegated action succeeding, a denied action failing safely with a spoken-appropriate message, a duplicate/retry not double-booking, and a trace being followable end to end afterwards. For each item state the precondition, the exact action, and the observable expected result — not "verify it works". Include the rollback drill: switch a channel to specialist routing, then back, and confirm nothing was destroyed. Mark clearly which items require production access and therefore belong to the Phase 136 human gate rather than to this phase.</action>
  <verify>test -f docs/agents/uat-checklist.md</verify>
</task>

</tasks>

<success_criteria>A CI workflow runs the gate and blocks on failure without touching the deploy path; the UAT checklist is followable by a non-author and separates what can be checked now from what needs production access.</success_criteria>
