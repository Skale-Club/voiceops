---
phase: 135-release-verification-and-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/vapi-latency-profile.test.ts
  - docs/agents/latency-profile.md
autonomous: true
requirements: [TEST-03]
---

<objective>
Produce a p95 latency figure for the Vapi ingress to specialist to tool-result path that
is reproducible, because it is measured against a profile someone wrote down.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the test profile before measuring anything</name>
  <files>docs/agents/latency-profile.md</files>
  <action>A p95 without a stated profile is not a reproducible claim. Document, before writing the test: exactly which boundaries are real and which are simulated, and why each choice is defensible for a latency claim; the fixed per-boundary latencies the simulation injects and where those numbers come from; the number of iterations and how p95 is computed from them; what "simple voice lookup" means concretely as a scenario; and what this profile deliberately does NOT prove — network transit to Vapi, real model inference time, real provider latency. State the 5-second target and the hardware assumption. Be honest that a deterministic simulated profile measures orchestration overhead, not end-to-end wall time against live vendors.</action>
  <verify>test -f docs/agents/latency-profile.md</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure the path and fail when the target is missed</name>
  <files>tests/vapi-latency-profile.test.ts</files>
  <action>Exercise the documented scenario end to end through the real orchestration code — trusted ingress resolution, specialist routing, the partner budget and channel ceiling, deterministic tool execution — with only the boundaries the profile names as simulated actually simulated. Run the documented iteration count, compute p95 as the profile defines it, and assert it against the 5-second target so the test FAILS rather than merely reporting a number. Keep the run deterministic and fast enough to belong in a gate: no live database, no network, no real model call. Report the measured figure in the failure message so a regression tells the reader how far off it was.</action>
  <verify>npx vitest run tests/vapi-latency-profile.test.ts</verify>
</task>

</tasks>

<success_criteria>A written profile states what is measured and what is not; the test walks the real orchestration path, computes p95 as documented, and fails when the 5-second target is missed.</success_criteria>
