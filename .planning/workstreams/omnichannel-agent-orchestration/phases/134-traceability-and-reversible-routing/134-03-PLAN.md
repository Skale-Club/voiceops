---
phase: 134-traceability-and-reversible-routing
plan: 03
type: execute
wave: 2
depends_on: [134-01, 134-02]
files_modified:
  - src/lib/agent-runtime/invocations.ts
  - src/lib/agent-runtime/run-agent.ts
  - src/lib/agent-runtime/execute-workflow-tool.ts
  - src/lib/agent-runtime/redact.ts
  - src/lib/agent-runtime/observability.ts
  - tests/invocation-partner-calls.test.ts
  - tests/invocation-redaction.test.ts
autonomous: true
requirements: [OBS-02]
---

<objective>
Make the persisted invocation record answer why a request stopped, what delegation it
actually performed, and what it cost — without writing secrets or needless personal data.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate partner_calls and record denials as denials</name>
  <files>src/lib/agent-runtime/invocations.ts, src/lib/agent-runtime/run-agent.ts, tests/invocation-partner-calls.test.ts</files>
  <action>Write the `partner_calls` column, which exists and is named by OBS-02 but has never been written: the edges actually traversed, their timing, and their outcome. Record every denial class introduced in Phases 132 and 133 — partner-edge denial, direct-tool denial, cycle, depth, disallowed channel, call count, timeout, channel model-invocation ceiling, idempotency conflict, and abandoned ownership — with its reason, keeping a denial distinguishable from an error, since a denied call is a deliberate refusal rather than a failure. Make an idempotency replay visible as a replay rather than as a fresh execution. Reflect a nested specialist failure in the parent invocation's status instead of swallowing it.</action>
  <verify>npx vitest run tests/invocation-partner-calls.test.ts tests/agent-delegation.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redact before persistence</name>
  <files>src/lib/agent-runtime/redact.ts, src/lib/agent-runtime/invocations.ts, tests/invocation-redaction.test.ts</files>
  <action>Add a redaction step applied before an observability row is written — never as a display-time filter — covering credential material and unnecessary personal data in `user_message`, `assistant_reply`, `tool_calls`, and `partner_calls`. Test it against realistic payloads, including nested structures. Do not touch `src/lib/crypto.ts` and do not change any encryption format; this is about not writing secrets into observability rows.</action>
  <verify>npx vitest run tests/invocation-redaction.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread the trace to the workflow boundary and read it back</name>
  <files>src/lib/agent-runtime/execute-workflow-tool.ts, src/lib/agent-runtime/observability.ts, tests/invocation-partner-calls.test.ts</files>
  <action>Pass the current trace and invocation identity from the agent tool path into the 134-01 logging fields so an agent-triggered workflow run is joinable to the invocation that caused it. Extend the observability read side so one query walks channel ingress, entry agent, nested specialists, workflow run, and Action Engine execution. Prove it with a test that walks the join, not one that asserts the columns exist.</action>
  <verify>npx vitest run tests/invocation-partner-calls.test.ts tests/workflow-run-trace-linkage.test.ts tests/agent-delegation.test.ts</verify>
</task>

</tasks>

<success_criteria>One trace walks ingress through provider result; partner_calls reflects real delegation; every denial class is recorded and distinguishable from an error; no credential or needless personal data reaches a persisted row.</success_criteria>
