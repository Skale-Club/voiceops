---
phase: 134-traceability-and-reversible-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/1292_workflow_run_trace_linkage.sql
  - src/types/database.ts
  - src/lib/workflows/log-tool-run.ts
  - src/app/api/vapi/tools/route.ts
  - tests/workflow-run-trace-linkage.test.ts
autonomous: true
requirements: [OBS-01]
---

<objective>
Close the one place the trace is severable: give a workflow run the trace and invocation
identity that lets an operator join it to the agent side.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trace linkage columns to workflow_runs</name>
  <files>supabase/migrations/1292_workflow_run_trace_linkage.sql, src/types/database.ts, tests/workflow-run-trace-linkage.test.ts</files>
  <action>Author migration 1292 adding a nullable trace id and a nullable agent-invocation reference to `workflow_runs`, with the invocation reference constrained to the same organization and behaving safely if the invocation row is later removed. Index what the join will actually be queried by. Both columns stay nullable: cron, campaign, and manual runs legitimately have no agent invocation, and existing rows must keep working untouched. Make it idempotent. Update `src/types/database.ts` by hand. Do not apply the migration.</action>
  <verify>npx vitest run tests/workflow-run-trace-linkage.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry the identity through the logging path</name>
  <files>src/lib/workflows/log-tool-run.ts, src/app/api/vapi/tools/route.ts, tests/workflow-run-trace-linkage.test.ts</files>
  <action>Widen `ToolRunLogInput` with optional trace and invocation identifiers and persist them. Optional, so every existing caller keeps compiling and behaving identically. Pass what the Vapi tool webhook already holds. That route is latency-sensitive and was hardened in Phase 133 — change only what carrying the identifier requires, and do not disturb its HTTP 200 contract, its per-call isolation, or its idempotency guard.</action>
  <verify>npx vitest run tests/workflow-run-trace-linkage.test.ts tests/vapi-tools-http200-contract.test.ts tests/vapi-tools-idempotency.test.ts tests/vapi-tools-multicall.test.ts</verify>
</task>

</tasks>

<success_criteria>A workflow run can be joined to the agent invocation and trace that caused it; runs with no agent origin still record cleanly; the Vapi route's Phase 133 guarantees are untouched.</success_criteria>
