---
phase: 134-traceability-and-reversible-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/1293_channel_routing_mode.sql
  - src/types/database.ts
  - src/lib/agent-runtime/routing-mode.ts
  - src/lib/agents/zod-schemas.ts
  - tests/channel-routing-mode.test.ts
autonomous: true
requirements: [ROLL-02]
---

<objective>
Let an operator move one channel at a time between legacy and specialist routing, and
back, without the rollback destroying anything.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a per-channel routing mode that defaults to legacy</name>
  <files>supabase/migrations/1293_channel_routing_mode.sql, src/types/database.ts, src/lib/agents/zod-schemas.ts, tests/channel-routing-mode.test.ts</files>
  <action>Author migration 1293 storing a routing mode per organization and channel, defaulting to legacy so no organization is silently migrated and existing rows need no backfill. Scope it by organization with RLS consistent with the neighbouring agent tables. Do not overload `routing_mode` in the calls settings actions — that is an unrelated call-handling setting (browser / phone_forward) and must stay separate. Add the matching zod schema and hand-update `src/types/database.ts`. Make the migration idempotent and do not apply it.</action>
  <verify>npx vitest run tests/channel-routing-mode.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve the mode cheaply and prove rollback destroys nothing</name>
  <files>src/lib/agent-runtime/routing-mode.ts, tests/channel-routing-mode.test.ts</files>
  <action>Add a resolver the ingress path can call without a model call, returning legacy when nothing is configured, when the row is missing, or when the value is unrecognised — unknown input must never be read as "enable specialist". Voice and text resolve independently. Prove by test that flipping a channel to specialist and back leaves agents, mappings, workflows, and prior invocation rows untouched: the switch selects which path reads configuration, it never writes configuration. Build the resolver only — do not wire it into any live route.</action>
  <verify>npx vitest run tests/channel-routing-mode.test.ts tests/agent-invocation-gateway.test.ts</verify>
</task>

</tasks>

<success_criteria>Each channel switches independently, defaults to legacy, falls back to legacy on unknown or missing configuration, and rolls back without deleting agents, mappings, workflows, or invocation history.</success_criteria>
