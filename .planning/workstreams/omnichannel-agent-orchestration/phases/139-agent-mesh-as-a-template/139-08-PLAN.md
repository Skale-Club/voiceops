---
phase: 139-agent-mesh-as-a-template
plan: 08
type: execute
wave: 3
depends_on: ["139-05", "139-06", "139-07"]
files_modified:
  - tests/org-templates-agents-e2e.test.ts
autonomous: false
requirements: [TMPL-01]

must_haves:
  truths:
    - "Capturing Cuts & Culture's mesh and installing it into a scratch org, using only the product UI, yields a working mesh with no SQL — 6 agents each with an active prompt version, 7 edges, 10 delegated grants of which exactly 3 are writes all on the Booking specialist."
    - "The installed prompts name the target business, not Cuts & Culture."
    - "Re-running the install changes nothing."
    - "Installing does not enable specialist routing anywhere."
  artifacts:
    - path: "tests/org-templates-agents-e2e.test.ts"
      provides: "a mocked, fixture-driven proof of the full capture -> install pipeline, using the canary JSON as a test fixture only (per 139-CONTEXT.md's locked decision: the JSON is not the source of truth)"
---

<objective>
Prove the full `agents` asset-group pipeline end to end against the shape it must
reproduce — the known-good Cuts & Culture mesh (6 agents, 7 edges, 10 delegated grants, 3
writes all on Booking) — using a mocked Supabase double so the assertion is exact and
repeatable, then have a human operator confirm the same thing through the real product UI
against the real live tenant, which no mock can substitute for.

Purpose: 137-VERIFICATION.md's own lesson, stated plainly: "Row counts and unit tests said
the mesh existed; nothing before this plan had exercised it" — mocked unit tests in 139-01
through 139-07 each prove their own function correct in isolation, but none of them proves
capture and install compose correctly end to end, and none of them can prove the real
Organization-Templates UI, driven by a real operator, produces the promised result against
the real Cuts & Culture organization. This plan is deliberately the last one, after every
other file in the phase already has its own unit coverage, and it does not touch any
production file — only a test file plus a checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/omnichannel-agent-orchestration/phases/139-agent-mesh-as-a-template/139-CONTEXT.md
@.planning/workstreams/omnichannel-agent-orchestration/canary/cuts-and-culture.json
@tests/canary-graph-shape.test.ts

<interfaces>
Per 139-CONTEXT.md's locked decisions: "The canary JSON under .planning/ becomes a fixture
for tests, not the source of truth." Use it ONLY to build the mocked fixture's shape (6
agents, 7 edges) for this test — do NOT assert the test's expected grant/write counts
against whatever numbers currently sit in that JSON file, which has drifted from the live
database before (see 137-VERIFICATION.md's "Live count: 10 edge grants (3 write)" versus
the JSON's own larger count after later commits mirrored unrelated direct-grant changes
into it). Assert against the counts stated in 139-CONTEXT.md's "Verification focus" instead:
6 agents, 7 edges, 10 delegated grants, exactly 3 writes, all on the Booking-shaped
specialist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mocked capture-then-install roundtrip against the known-good shape</name>
  <files>tests/org-templates-agents-e2e.test.ts</files>
  <action>
Write a test that builds an in-memory fake Supabase source org shaped like the shipped
Cuts & Culture mesh (6 agents keyed by the canary JSON's agent slugs/roles for readability,
7 partner edges, direct + delegated grants such that exactly 10 delegated workflow grants
exist and exactly 3 of them — all on the Booking-shaped specialist's incoming edge — are
`book_appointment`/`reschedule_appointment`/`cancel_appointment`), runs
`captureOrgSnapshot(fakeSource, ['agents', 'workflows'])`, then
`installSnapshotIntoOrg(fakeAdmin, targetOrgId, snapshot, ['agents', 'workflows'],
createdBy)` against a second fake org, and asserts:

1. The target org ends up with 6 agents, every one with a non-null
   `active_prompt_version_id`.
2. Exactly 7 `agent_partners` rows and 10 `agent_partner_workflow_grants` rows.
3. Exactly 3 of those 10 delegated grants resolve to a workflow whose captured tool_name is
   one of `book_appointment` / `reschedule_appointment` / `cancel_appointment`, and all 3
   target the same partner agent (the Booking-shaped one).
4. An orchestrator-role agent's rendered prompt in the target org contains the TARGET org's
   fixture business name and does not contain the source fixture's business name anywhere.
5. Running the identical `installSnapshotIntoOrg()` call a second time against the same
   target org produces identical row counts (idempotent) — reuse the same fake client
   instance so state persists between the two calls within the test.
6. Zero calls were made to `agent_channel_routing_modes` across the entire capture+install
   run (the routing-modes table is never touched by this pipeline).

This test never touches a real database and never calls the Vapi API — it is a same-process
assertion over the fake client's recorded state, in the same style as
`tests/canary-graph-shape.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/org-templates-agents-e2e.test.ts</automated>
  </verify>
  <done>All assertions in tests/org-templates-agents-e2e.test.ts pass, matching the known-good shape (6/7/10/3) exactly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirmation against the real live tenant</name>
  <files>none — verification only, no files modified</files>
  <action>
Walk through the real Organization Templates and Agents UI against the real Cuts & Culture
organization, per the how-to-verify steps below, and confirm the resulting new organization
has a working 6-agent mesh with real prompts naming itself.
  </action>
  <what-built>
Every automated piece of the `agents` template asset group: capture, prompt-token
rendering, idempotent install with mandatory prompt versions, the outbound Vapi sync, and
the two operator UI surfaces (channel routing modes; agent counts on the templates page and
a push-to-Vapi button on the assistants page). The mocked test above proves the pipeline's
shape is correct against the known-good numbers. What no automated test in this phase can
prove is that the real Settings UI, run by a real operator against the real Cuts & Culture
organization, produces the same result — this is exactly the gap 137-VERIFICATION.md
flagged when it said mocked tests alone missed a real defect (the missing prompt versions).
  </what-built>
  <how-to-verify>
1. In Settings → Organization Templates, create a template from Cuts & Culture with the
   `agents` and `workflows` asset groups selected (plus any others you also want).
2. Confirm the template list shows an agent count matching Cuts & Culture's mesh (6).
3. Create a new organization from that template (a scratch/throwaway name is fine).
4. In the new org, open Agents: confirm 6 agents exist, each shows a real prompt (not an
   error/blank state, which is what an agent with no active prompt version looks like), and
   the orchestrator's prompt names the NEW org, not Cuts & Culture.
5. Confirm (Settings → Agents → Settings dialog) the new org's channel routing modes are
   all "Legacy (default)" and no channel default silently points anywhere unexpected.
6. Optional but recommended: repeat step 3 (create a second org from the same template) and
   confirm nothing about the FIRST new org changed.
  </how-to-verify>
  <resume-signal>Type "approved" once the new organization has a working 6-agent mesh with real prompts naming itself, or describe what's wrong.</resume-signal>
  <verify>
    <automated>MISSING — this step is inherently manual (real operator, real UI, real live tenant); no automated command substitutes for it. See <how-to-verify>.</automated>
  </verify>
  <done>Operator has typed "approved" confirming the new organization's 6-agent mesh is real and working, or has reported specific issues to address before the phase can be marked done.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/org-templates-agents-e2e.test.ts` passes with the exact 6/7/10/3
shape. The human-verify checkpoint is explicitly answered before this plan is considered
done — an unanswered or skipped checkpoint means this plan is not complete, regardless of
the automated test's result.
</verification>

<success_criteria>
The `agents` asset group reproduces the known-good Cuts & Culture shape (6 agents, 7 edges,
10 delegated grants, 3 writes on Booking) through both a mocked pipeline test and a real
operator walking through the actual product UI against the actual live tenant, closing the
exact gap the phase exists to close: standing up the mesh for a new tenant is now an
operator action, not a sequence of scripts and hand-written SQL.
</success_criteria>
