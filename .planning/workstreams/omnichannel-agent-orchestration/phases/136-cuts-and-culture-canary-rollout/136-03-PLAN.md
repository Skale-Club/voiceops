---
phase: 136-cuts-and-culture-canary-rollout
plan: 03
type: execute
wave: 2
depends_on: [136-01, 136-02]
files_modified:
  - docs/agents/canary-activation-runbook.md
autonomous: true
requirements: [ROLL-03]
---

<objective>
Hand a person the ordered, reversible sequence that takes the canary live, with an abort
step at every stage — because every remaining step of ROLL-03 requires production access
and none of them may be performed by an agent.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Write the activation runbook</name>
  <files>docs/agents/canary-activation-runbook.md</files>
  <action>Write the ordered production sequence: apply migrations 1290-1294 with `npx supabase db push`; run the 136-02 provisioning script as a dry run and review its output; run it with the apply flag against the Cuts &amp; Culture organization; bind the Vapi assistant to the entry orchestrator; flip the voice routing mode to specialist and observe; then the widget channel; then place one real booking and follow its trace end to end. For each step state the precondition, the exact command or action, the observable success signal, and — most importantly — the abort step: how to reverse that specific step if the signal is wrong. Note explicitly that rollback of the routing flip is non-destructive by construction and that reverting a routing row does not remove agents, mappings, workflows, or invocation history. Reference the Phase 135 UAT checklist for what to observe rather than duplicating it. State plainly at the top that every step requires a human and that no agent has performed any of them.</action>
  <verify>test -f docs/agents/canary-activation-runbook.md</verify>
</task>

<task type="auto">
  <name>Task 2: Record what the phase proved and what it could not</name>
  <files>docs/agents/canary-activation-runbook.md</files>
  <action>Close the runbook with an honest split: what is proven by test in this repository today — graph shape, the only-Booking-writes grant, the shared Availability specialist, legacy-by-default routing, idempotent booking mutations — and what remains unproven until the canary actually runs, namely that a real widget interaction and a real Vapi interaction reach the same specialist in production, and that a real booking completes idempotently with a complete trace. Do not describe unrun steps as done, and do not let the runbook's completeness imply the canary is live.</action>
  <verify>test -f docs/agents/canary-activation-runbook.md</verify>
</task>

</tasks>

<success_criteria>A person can take the canary live from the runbook alone, abort at any stage, and tell from it exactly which ROLL-03 claims are proven and which await the live run.</success_criteria>
