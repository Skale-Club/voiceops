---
phase: 136-cuts-and-culture-canary-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent-runtime/invocation-gateway.ts
  - src/lib/agent-runtime/routing-mode.ts
  - tests/channel-routing-wiring.test.ts
autonomous: true
requirements: [ROLL-02]
---

<objective>
Make the routing switch actually switch something, while leaving every organization on
legacy so that merging this changes nobody's behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consult the routing mode at the trusted boundary</name>
  <files>src/lib/agent-runtime/invocation-gateway.ts, src/lib/agent-runtime/routing-mode.ts, tests/channel-routing-wiring.test.ts</files>
  <action>Phase 134 built `resolveChannelRoutingMode()` and deliberately wired it into nothing, so an operator can flip it today and observe no difference. Consult it at the trusted boundary so that a channel in specialist mode takes the Phase 132 trusted-route path and a channel in legacy mode behaves exactly as it does now. The legacy branch must be byte-for-byte the current behavior — this is a switch, not a rewrite of the path it switches away from. Resolve the mode once per invocation, not per tool call, and keep it off the hot path for legacy callers.</action>
  <verify>npx vitest run tests/channel-routing-wiring.test.ts tests/agent-invocation-gateway.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove merging this is a no-op for everyone</name>
  <files>tests/channel-routing-wiring.test.ts</files>
  <action>Assert the properties that make this safe to merge, because they are the whole argument for merging it before the canary exists. With no routing row present, every channel resolves to legacy and the specialist path is never entered. The same holds when the lookup errors, when the value is unrecognised, and when the row exists but names legacy. Flipping voice to specialist changes voice and leaves widget untouched, and the reverse. Assert the specialist path is entered only on an explicit specialist value — never inferred, never defaulted into.</action>
  <verify>npx vitest run tests/channel-routing-wiring.test.ts tests/agent-invocation-gateway.test.ts tests/channel-routing-mode.test.ts tests/agent-delegation.test.ts</verify>
</task>

</tasks>

<success_criteria>The switch selects a real path; absent, errored, unknown and legacy configuration all resolve to today's behavior; channels move independently; the specialist path is reachable only by an explicit opt-in value.</success_criteria>
