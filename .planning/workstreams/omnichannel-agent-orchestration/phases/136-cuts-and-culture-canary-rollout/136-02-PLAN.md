---
phase: 136-cuts-and-culture-canary-rollout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/provision-canary-graph.ts
  - .planning/workstreams/omnichannel-agent-orchestration/canary/cuts-and-culture.json
  - tests/canary-graph-shape.test.ts
autonomous: true
requirements: [ROLL-01]
---

<objective>
Express the Cuts & Culture specialist graph as a reviewable, idempotent, tenant-scoped
artifact that has not been run, and prove its shape and its least-privilege claim by test
rather than by reading the configuration and trusting it.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare the graph as tenant-scoped data</name>
  <files>.planning/workstreams/omnichannel-agent-orchestration/canary/cuts-and-culture.json, tests/canary-graph-shape.test.ts</files>
  <action>Declare the entry orchestrator plus the Services, Pricing, Availability, Customer and Booking specialists, the partner edges between them, each edge's allowed channels and budgets, and the delegated workflow grants. Only Booking may carry an Xkedule write grant; the other four are read-only. Voice and widget must reference the SAME Availability specialist — one definition, two channels, which is the point of the whole graph. Keep this OUT of `supabase/seeds/workflows/`: that path holds platform defaults and is validated in CI, and the locked decision is that this tenant is an isolated canary, never a product default. Assert the shape, the channel coverage, and the single-Availability property by test.</action>
  <verify>npx vitest run tests/canary-graph-shape.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provision it safely, and by default not at all</name>
  <files>scripts/provision-canary-graph.ts, tests/canary-graph-shape.test.ts</files>
  <action>Write an idempotent provisioning script following the house pattern in `scripts/seed-demo-org.ts`. It must be a DRY RUN by default, printing what it would change and writing nothing. Writing requires both an explicit apply flag and an explicit organization identifier, and the script must refuse to run against any organization other than the one named on the command line — no inferred default, no "current org" fallback. Re-running after a successful apply must be a no-op. Assert by test that the script writes only within the target organization, that only Booking ends up with an Xkedule write grant, and that a dry run performs no writes. Do NOT run this script against any real organization.</action>
  <verify>npx vitest run tests/canary-graph-shape.test.ts</verify>
</task>

</tasks>

<success_criteria>The graph is declared outside the platform seed path with one shared Availability specialist; only Booking holds an Xkedule write grant, proven against the grant data; the script is idempotent, dry-run by default, refuses a non-target organization, and has not been run.</success_criteria>
