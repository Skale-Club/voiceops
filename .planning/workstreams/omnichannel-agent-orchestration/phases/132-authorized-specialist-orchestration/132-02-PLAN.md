---
phase: 132-authorized-specialist-orchestration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/1291_authorized_agent_partner_edges.sql
  - src/types/database.ts
  - src/lib/agent-runtime/resolve-partner-edge.ts
  - src/lib/agents/zod-schemas.ts
  - tests/agent-partner-edge-authz.test.ts
autonomous: true
requirements: [ROUT-03, AUTHZ-01, AUTHZ-02, AUTHZ-03]
---

<objective>
Give every agent-to-agent edge tenant-safe channel, capability, and budget policy without conflating delegation permission with direct workflow ownership.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalized tenant-safe partner-edge policy schema</name>
  <files>supabase/migrations/1291_authorized_agent_partner_edges.sql, src/types/database.ts, tests/agent-partner-edge-authz.test.ts</files>
  <action>Add composite same-organization foreign keys for both agent endpoints. Add explicit allowed channels and bounded max calls/depth/timeout policy. Represent delegated workflows with a normalized grant table whose edge and workflow are constrained to the same organization; do not use an unverifiable UUID array. Preserve existing rows with a documented conservative default and perform no tenant-specific inserts. Do not apply the migration.</action>
  <verify>npx vitest run tests/agent-partner-edge-authz.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve one edge through a fail-closed preflight contract</name>
  <files>src/lib/agent-runtime/resolve-partner-edge.ts, src/lib/agents/zod-schemas.ts, tests/agent-partner-edge-authz.test.ts</files>
  <action>Return a typed allow/deny decision after checking organization, active source and target, channel, explicit edge, limits, and delegated workflow grants. Ignore payload-provided identity. Ensure a delegation grant never creates a direct tool grant and missing/malformed policy fails closed for side effects.</action>
  <verify>npx vitest run tests/agent-partner-edge-authz.test.ts tests/agents/zod-schemas.test.ts</verify>
</task>

</tasks>

<success_criteria>Cross-org edges are impossible at the database boundary and runtime preflight distinguishes traversal permission from direct tool ownership.</success_criteria>

