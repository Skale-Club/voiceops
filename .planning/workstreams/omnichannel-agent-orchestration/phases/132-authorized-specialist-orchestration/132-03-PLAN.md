---
phase: 132-authorized-specialist-orchestration
plan: 03
type: execute
wave: 2
depends_on: [132-01, 132-02]
files_modified:
  - src/lib/agent-runtime/run-agent.ts
  - src/lib/agent-runtime/build-workflow-tools.ts
  - src/lib/agent-runtime/resolve-agent-tool.ts
  - src/lib/agent-runtime/guardrails.ts
  - src/lib/knowledge/query-knowledge.ts
  - tests/agent-delegation.test.ts
  - tests/agent-workflow-tools.test.ts
  - tests/agent-knowledge-scope.test.ts
autonomous: true
requirements: [KNOW-01]
---

<objective>
Activate edge-based least privilege and enforce each specialist's knowledge scope identically in blocking and streaming execution.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace ancestor direct-tool intersection</name>
  <files>src/lib/agent-runtime/run-agent.ts, src/lib/agent-runtime/build-workflow-tools.ts, src/lib/agent-runtime/resolve-agent-tool.ts, src/lib/agent-runtime/guardrails.ts, tests/agent-delegation.test.ts, tests/agent-workflow-tools.test.ts</files>
  <action>Use the current edge preflight for every child call and workflow execution. Effective delegated authority is specialist direct grant intersected with current edge grant and channel policy. Remove ancestor direct-workflow intersection only after denial tests cover direct execution, cross-org, inactive, channel, cycle, depth, call-count, timeout, and ungranted workflow cases. Share mutable budget state across the invocation tree.</action>
  <verify>npx vitest run tests/agent-delegation.test.ts tests/agent-workflow-tools.test.ts tests/agent-partner-edge-authz.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce resolved kb_scope at retrieval</name>
  <files>src/lib/knowledge/query-knowledge.ts, src/lib/agent-runtime/run-agent.ts, tests/agent-knowledge-scope.test.ts</files>
  <action>Implement null as full-org legacy scope, empty array as automatic retrieval disabled, and non-empty scope as tenant-owned filtered retrieval. Pass only resolveAgent's scope in both runtime paths. Never accept a scope from handoff or ingress metadata.</action>
  <verify>npx vitest run tests/agent-knowledge-scope.test.ts tests/agent-delegation.test.ts</verify>
</task>

</tasks>

<success_criteria>An orchestrator can delegate without owning specialist tools, but the specialist cannot exceed its own and the edge's grants; scoped knowledge cannot leak across specialists or tenants.</success_criteria>

