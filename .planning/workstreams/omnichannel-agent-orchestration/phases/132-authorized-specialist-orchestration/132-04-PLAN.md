---
phase: 132-authorized-specialist-orchestration
plan: 04
type: execute
wave: 3
depends_on: [132-03]
files_modified:
  - src/lib/agent-runtime/invocation-gateway.ts
  - src/lib/agent-runtime/resolve-specialist-route.ts
  - src/lib/llm/openrouter.ts
  - src/app/api/email-templates/generate/route.ts
  - src/app/api/ads/memories/extract/route.ts
  - src/app/(dashboard)/workflows/flows/_actions/ai-build.ts
  - src/lib/prospects/qualify-llm.ts
  - src/lib/knowledge/query-knowledge.ts
  - src/app/(dashboard)/email-marketing/_actions/generate.ts
  - tests/agent-specialist-routing.test.ts
  - tests/openrouter-provider-policy.test.ts
autonomous: true
requirements: [ROUT-02, MODEL-01, MODEL-02]
---

<objective>
Resolve explicit trusted intents directly to specialists and consolidate all Xphere-owned generation on tenant-first/platform-fallback OpenRouter access.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add channel-neutral trusted specialist route resolution</name>
  <files>src/lib/agent-runtime/resolve-specialist-route.ts, src/lib/agent-runtime/invocation-gateway.ts, tests/agent-specialist-routing.test.ts</files>
  <action>Resolve a trusted explicit intent/function mapping to an active same-org specialist and invoke it directly through invokeAgent without a router model call. Route ambiguity to the configured entry agent. Keep mapping channel-neutral and tenant-configured, not hardcoded to Vapi or Cuts & Culture. Build and test the resolver only; do not cut over /api/vapi/tools.</action>
  <verify>npx vitest run tests/agent-specialist-routing.test.ts tests/agent-invocation-gateway.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize OpenRouter generation and add drift guard</name>
  <files>src/lib/llm/openrouter.ts, direct generative call sites, tests/openrouter-provider-policy.test.ts</files>
  <action>Create one server-side provider resolver/factory with tenant OpenRouter first and platform OpenRouter fallback. Migrate every inventoried generative call site; OpenAI SDK transport is allowed only with OpenRouter base URL. Remove direct Anthropic generation and fallback. Classify embedding modules explicitly and keep their model/vector contract unchanged. Add a static contract test that fails on new direct generative Anthropic/OpenAI clients outside documented embedding exceptions.</action>
  <verify>npx vitest run tests/openrouter-provider-policy.test.ts</verify>
</task>

<task type="auto">
  <name>Task 3: Run Phase 132 compatibility gate</name>
  <files>all Phase 132 files and focused regression tests</files>
  <action>Run handoff, edge auth, delegation, workflow-tool, knowledge-scope, direct-routing, provider-policy, gateway, Action Engine, and Vapi suites together. Run the production build with a 4 GB Node heap. Verify /api/vapi/tools remains uncut and migrations 1290/1291 remain unapplied. Create summaries and 132-VERIFICATION.md.</action>
  <verify>$env:NODE_OPTIONS='--max-old-space-size=4096'; npm run build</verify>
</task>

</tasks>

<success_criteria>Explicit intents reach specialists without an extra model call, ambiguous requests retain one orchestrator, generative calls use OpenRouter, embeddings remain compatible, and no production traffic is cut over.</success_criteria>

