---
phase: 132-authorized-specialist-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent-runtime/handoff.ts
  - src/lib/agent-runtime/types.ts
  - src/lib/agent-runtime/run-agent.ts
  - tests/agent-handoff-contract.test.ts
  - tests/agent-delegation.test.ts
autonomous: true
requirements: [ROUT-01, ROUT-04, ROUT-05, KNOW-02]
---

<objective>
Replace unrestricted partner payloads and string-only results with allow-listed typed handoff and specialist-result contracts while preserving one response owner.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build pure handoff validation and typed result contracts</name>
  <files>src/lib/agent-runtime/handoff.ts, src/lib/agent-runtime/types.ts, tests/agent-handoff-contract.test.ts</files>
  <action>Create a strict handoff input containing only source agent, intent, approved extracted parameters, bounded summary, and at most three recent user/assistant messages. Recursively reject objects and arrays containing identity, organization, agent, role/system/instruction, secret/credential/token/API-key, runtime-control, or prototype-pollution keys. Add a discriminated specialist result union for success, business_failure, retryable_failure, and handoff. Keep errors channel-safe and exclude internal reasoning.</action>
  <verify>npx vitest run tests/agent-handoff-contract.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate contracts into partner tools</name>
  <files>src/lib/agent-runtime/run-agent.ts, tests/agent-delegation.test.ts</files>
  <action>Replace the local deny-list validator and unrestricted dynamic-tool schema with the pure contract. Normalize the child AgentRunResult into the typed specialist result and let only the caller convert it to channel prose. Preserve trace, parent invocation, depth, visited set, streaming events, and blocking child execution.</action>
  <verify>npx vitest run tests/agent-handoff-contract.test.ts tests/agent-delegation.test.ts</verify>
</task>

</tasks>

<success_criteria>Nested injection and identity override payloads are denied before child invocation; valid handoffs and all legacy delegation tests pass; exactly one caller owns the channel response.</success_criteria>

