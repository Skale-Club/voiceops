---
phase: 131-trusted-omnichannel-invocation-foundation
plan: 03
type: execute
wave: 2
depends_on: [131-01, 131-02]
files_modified:
  - src/lib/agent-runtime/types.ts
  - src/lib/agent-runtime/invocation-gateway.ts
  - src/lib/agent-runtime/index.ts
  - src/lib/vapi/end-of-call.ts
  - tests/agent-invocation-gateway.test.ts
  - tests/vapi-call-events.test.ts
autonomous: true
requirements: [AIGW-01, AIGW-02, AIGW-03, AIGW-04, AUTHZ-04]

must_haves:
  truths:
    - "Voice and web widget can invoke the existing runAgent runtime through one typed gateway"
    - "Trusted route identity is structurally separate from untrusted message metadata and cannot be overridden by payload fields"
    - "Vapi assistant resolution returns entry_agent_id only from the trusted active mapping that owns the organization"
    - "No production Vapi tools request is routed through the gateway in Phase 131"
  artifacts:
    - path: "src/lib/agent-runtime/invocation-gateway.ts"
      provides: "Shared typed ingress contract and runAgent adapter"
      exports: ["invokeAgent", "AgentInvocationEnvelope", "TrustedAgentRoute"]
    - path: "tests/agent-invocation-gateway.test.ts"
      provides: "Shared-channel, identity-override, trace, and idempotency contract tests"
  key_links:
    - from: "src/lib/agent-runtime/invocation-gateway.ts"
      to: "src/lib/agent-runtime/run-agent.ts"
      via: "invokeAgent delegates to runAgent using only TrustedAgentRoute identity"
      pattern: "runAgent\("
    - from: "src/lib/vapi/end-of-call.ts"
      to: "assistant_mappings.entry_agent_id"
      via: "active assistant mapping select"
      pattern: "entry_agent_id"
---

<objective>
Create one trusted typed entry point around the existing agent runtime and expose the optional Vapi entry-agent binding through tenant resolution. Prove shared voice/widget invocation and identity isolation in tests, while deliberately leaving `/api/vapi/tools` on its legacy workflow path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/omnichannel-agent-orchestration/phases/131-trusted-omnichannel-invocation-foundation/131-CONTEXT.md
@src/lib/agent-runtime/types.ts
@src/lib/agent-runtime/run-agent.ts
@src/lib/agent-runtime/index.ts
@src/lib/vapi/end-of-call.ts
@src/app/api/vapi/tools/route.ts
@tests/agent-runtime-integration.test.ts
@tests/vapi-call-events.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the trusted omnichannel invocation envelope</name>
  <files>src/lib/agent-runtime/types.ts, src/lib/agent-runtime/invocation-gateway.ts, src/lib/agent-runtime/index.ts, tests/agent-invocation-gateway.test.ts</files>
  <read_first>
    - src/lib/agent-runtime/types.ts
    - src/lib/agent-runtime/run-agent.ts
    - src/lib/agent-runtime/index.ts
    - tests/agent-runtime-integration.test.ts
    - src/types/vapi.ts
  </read_first>
  <action>
    Add `TrustedAgentRoute` with exact trusted fields `orgId`, `agentId`, `channel`, `externalInteractionId`, optional `conversationId`, optional `sessionId`, optional `traceId`, and optional `idempotencyKey`. Add `AgentInvocationEnvelope` with `route: TrustedAgentRoute`, `input: { userMessage: string; intent?: string; locale?: string; actor?: { externalId?: string; contactId?: string; name?: string; phone?: string; email?: string }; metadata?: Record&lt;string, unknown&gt; }`, and runtime options `historyWindow`, `mode`, `stream`, `maxSteps`, `extraInstructions`. Create `invokeAgent(envelope)` in `invocation-gateway.ts`; generate UUID trace/idempotency values when trusted adapters omit them, and call the existing `runAgent` using only identity from `envelope.route`. Do not spread `input` or `metadata` into `AgentRunOptions`. Preserve blocking/streaming return behavior with overloads matching `runAgent`. Return or expose the normalized trace and ingress idempotency key in a typed wrapper without changing `AgentRunResult`; choose a wrapper that does not break existing callers. Export the new gateway/types from the runtime index. Tests must mock `runAgent` and prove: voice and web_widget both call it; route org/agent win even when input metadata contains `orgId`, `organization_id`, `agentId`, or `agent_id`; supplied trace/idempotency values are stable; missing values are UUIDs; actor/locale/intent metadata cannot become runtime identity; stream flag is forwarded.
  </action>
  <verify>
    <automated>npx vitest run tests/agent-invocation-gateway.test.ts</automated>
  </verify>
  <done>The gateway is the single new ingress adapter around runAgent and all identity-separation/channel tests pass without a real model call.</done>
  <acceptance_criteria>
    - `src/lib/agent-runtime/invocation-gateway.ts` exports `invokeAgent`
    - `src/lib/agent-runtime/types.ts` exports `TrustedAgentRoute` and `AgentInvocationEnvelope`
    - Tests explicitly include malicious metadata keys `organization_id` and `agent_id`
    - `npx vitest run tests/agent-invocation-gateway.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve the optional Vapi entry agent from the trusted active mapping</name>
  <files>src/lib/vapi/end-of-call.ts, tests/vapi-call-events.test.ts</files>
  <read_first>
    - src/lib/vapi/end-of-call.ts
    - src/lib/action-engine/resolve-org.ts
    - src/app/api/vapi/tools/route.ts
    - tests/vapi-call-events.test.ts
    - tests/action-engine.test.ts
  </read_first>
  <action>
    Extend `ResolvedCallOrg` with `entryAgentId: string | null`. Change the active assistant mapping select to `organization_id, entry_agent_id`. When the assistant mapping wins tenant resolution, return its nullable `entry_agent_id`; when resolution falls back to a Vapi-native or legacy phone-number row, return `entryAgentId: null`. Preserve phone-number same-tenant mismatch handling. Update every return in `resolveOrgForCall` and its tests. Add assertions for mapped entry agent, null mapping, number fallback, and assistant/number cross-tenant mismatch. Do not read agent identity from call metadata or tool arguments. Do not modify `src/app/api/vapi/tools/route.ts` except for a compile-only destructuring adjustment if TypeScript requires it; the route must not call `invokeAgent` in this phase.
  </action>
  <verify>
    <automated>npx vitest run tests/vapi-call-events.test.ts tests/action-engine.test.ts</automated>
  </verify>
  <done>Trusted Vapi resolution exposes a tenant-safe optional entry agent while every legacy/null path behaves as before.</done>
  <acceptance_criteria>
    - `ResolvedCallOrg` contains `entryAgentId: string | null`
    - The assistant mapping query selects `organization_id, entry_agent_id`
    - Every phone-only fallback returns `entryAgentId: null`
    - `src/app/api/vapi/tools/route.ts` contains no `invokeAgent` import or call
    - Focused tests exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run the complete Phase 131 compatibility gate</name>
  <files>tests/agent-invocation-gateway.test.ts, tests/action-engine.test.ts, tests/vapi-call-events.test.ts, tests/agents/zod-schemas.test.ts, tests/assistant-agent-binding.test.ts</files>
  <read_first>
    - All files modified by Plans 131-01, 131-02, and Tasks 1-2 of this plan
    - .planning/workstreams/omnichannel-agent-orchestration/REQUIREMENTS.md
  </read_first>
  <action>
    Run the five focused test files together, then run `npm run build`. Fix only defects within Phase 131 files. Verify by search that the production Vapi tools route has no gateway import and migration 1290 has no assistant-mapping data backfill. Record exact command results and durations in the plan summary. Do not run `supabase db push`.
  </action>
  <verify>
    <automated>npx vitest run tests/agent-invocation-gateway.test.ts tests/action-engine.test.ts tests/vapi-call-events.test.ts tests/agents/zod-schemas.test.ts tests/assistant-agent-binding.test.ts --testTimeout=10000 &amp;&amp; npm run build</automated>
  </verify>
  <done>All focused tests and the production build pass; the Vapi route remains legacy and migration 1290 remains unapplied.</done>
  <acceptance_criteria>
    - Combined focused Vitest command exits 0
    - `npm run build` exits 0
    - `rg "invokeAgent" src/app/api/vapi/tools/route.ts` returns no matches
    - `rg "UPDATE public.assistant_mappings|INSERT INTO public.assistant_mappings" supabase/migrations/1290_omnichannel_agent_invocation_foundation.sql` returns no matches
  </acceptance_criteria>
</task>

</tasks>

<verification>
The combined Phase 131 focused suite and `npm run build` pass. Manual inspection confirms the Vapi route is unchanged, mapping identity is server-derived, and no production data is mutated.
</verification>

<success_criteria>
Voice and text can share a typed internal invocation boundary with trusted tenant/agent identity, while the live Vapi route and Action Engine remain behaviorally unchanged and protected by a green baseline.
</success_criteria>

<output>
After completion, create `131-03-SUMMARY.md` in this phase directory.
</output>
