---
phase: 131-trusted-omnichannel-invocation-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/1290_omnichannel_agent_invocation_foundation.sql
  - src/types/database.ts
  - src/lib/agents/channels.ts
  - tests/agents/zod-schemas.test.ts
  - tests/assistant-agent-binding.test.ts
autonomous: true
requirements: [AIGW-02, AIGW-03, AIGW-04, AUTHZ-04]

must_haves:
  truths:
    - "Voice is a first-class agent channel accepted by database and application configuration"
    - "An assistant mapping may reference one entry agent only when both rows belong to the same organization"
    - "Existing assistant mappings remain valid with entry_agent_id NULL and no production assistant is backfilled"
    - "Authenticated assistant and agent configuration remains protected by existing organization RLS"
  artifacts:
    - path: "supabase/migrations/1290_omnichannel_agent_invocation_foundation.sql"
      provides: "voice enum value plus tenant-safe optional assistant-to-entry-agent binding"
      contains: "entry_agent_id"
    - path: "src/lib/agents/channels.ts"
      provides: "Voice channel registry and label"
      contains: "voice"
    - path: "tests/assistant-agent-binding.test.ts"
      provides: "Schema-contract coverage for same-org assistant binding"
  key_links:
    - from: "assistant_mappings.organization_id, assistant_mappings.entry_agent_id"
      to: "agents.organization_id, agents.id"
      via: "composite foreign key"
      pattern: "FOREIGN KEY \(organization_id, entry_agent_id\)"
---

<objective>
Add the minimum additive data model for shared voice/text agents: `voice` in the channel domain and a nullable tenant-safe Vapi assistant-to-entry-agent binding. Update TypeScript/application projections and test the schema contract without applying the migration to production.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/omnichannel-agent-orchestration/phases/131-trusted-omnichannel-invocation-foundation/131-CONTEXT.md
@supabase/migrations/001_foundation.sql
@supabase/migrations/034_agents.sql
@supabase/migrations/036_agent_channel_defaults.sql
@src/types/database.ts
@src/lib/agents/channels.ts
@src/lib/agents/zod-schemas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the additive voice channel and same-tenant assistant binding migration</name>
  <files>supabase/migrations/1290_omnichannel_agent_invocation_foundation.sql, tests/assistant-agent-binding.test.ts</files>
  <read_first>
    - supabase/migrations/001_foundation.sql
    - supabase/migrations/034_agents.sql
    - supabase/migrations/050_sms_channel.sql
    - supabase/migrations/1124_zernio_contract_hardening.sql
    - tests/agent-schema-rls-smoke.test.ts
    - tests/agents/rls.test.ts
  </read_first>
  <action>
    Create migration `1290_omnichannel_agent_invocation_foundation.sql`. Add enum value using `ALTER TYPE public.agent_channel ADD VALUE IF NOT EXISTS 'voice';`. Add `CONSTRAINT uniq_agents_organization_id_id UNIQUE (organization_id, id)` to `public.agents` inside an idempotent `DO` block that checks `pg_constraint`. Add nullable `entry_agent_id uuid` to `public.assistant_mappings`. Add composite foreign key `assistant_mappings_entry_agent_same_org_fkey` on `(organization_id, entry_agent_id)` referencing `public.agents(organization_id, id)` inside an idempotent constraint check, and add `idx_assistant_mappings_entry_agent_id`. Use the default `NO ACTION` delete behavior so a mapped agent cannot be hard-deleted while referenced; agents are normally deactivated. Do not insert or backfill any mapping. Do not alter existing RLS policies. Add a deterministic migration contract test that reads the migration text and asserts all exact clauses, including absence of `UPDATE public.assistant_mappings` and absence of `INSERT INTO public.assistant_mappings`. If the repository's real-DB test pattern can run without credentials, optionally add a soft-skipped integration case proving a cross-org composite FK rejection; the text contract test is mandatory.
  </action>
  <verify>
    <automated>npx vitest run tests/assistant-agent-binding.test.ts</automated>
  </verify>
  <done>The migration is append-only/idempotent, adds no data, and structurally prevents cross-organization entry-agent binding.</done>
  <acceptance_criteria>
    - Migration contains `ALTER TYPE public.agent_channel ADD VALUE IF NOT EXISTS 'voice'`
    - Migration contains `FOREIGN KEY (organization_id, entry_agent_id)` and `REFERENCES public.agents(organization_id, id)`
    - Migration does not contain an assistant-mapping `UPDATE` or `INSERT`
    - `npx vitest run tests/assistant-agent-binding.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project voice and entry_agent_id through TypeScript and agent configuration</name>
  <files>src/types/database.ts, src/lib/agents/channels.ts, tests/agents/zod-schemas.test.ts</files>
  <read_first>
    - src/types/database.ts
    - src/lib/agents/channels.ts
    - src/lib/agents/zod-schemas.ts
    - tests/agents/zod-schemas.test.ts
    - src/components/agents/channel-overrides-editor.tsx
  </read_first>
  <action>
    Update `src/types/database.ts` manually to mirror migration 1290: add `entry_agent_id: string | null` to assistant_mappings Row, optional nullable fields to Insert/Update, add the `assistant_mappings_entry_agent_same_org_fkey` relationship to agents, and add `'voice'` to the `AgentChannel` enum union and public Enums array if present. In `src/lib/agents/channels.ts`, add `voice` to `PUBLIC_AGENT_CHANNELS` and label it `Voice (Vapi)`. Because `agentSchema` derives from `AGENT_CHANNELS`, do not create a second validation list. Update `tests/agents/zod-schemas.test.ts` so enum-mirroring and a valid `voice` channel override are asserted. Keep `workflow` server-only and excluded from public channels.
  </action>
  <verify>
    <automated>npx vitest run tests/agents/zod-schemas.test.ts tests/assistant-agent-binding.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Database types, forms, validation, and labels accept voice while preserving existing channels and server-only workflow behavior.</done>
  <acceptance_criteria>
    - `src/lib/agents/channels.ts` contains `voice: 'Voice (Vapi)'`
    - `src/types/database.ts` contains `entry_agent_id: string | null`
    - `src/types/database.ts` AgentChannel includes `'voice'`
    - Focused tests and `npm run build` exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run both focused test files and `npm run build`. Inspect the migration to confirm no data mutation/backfill and no modification of migration history.
</verification>

<success_criteria>
Voice and optional assistant entry-agent binding exist as additive tenant-safe platform primitives, with legacy null behavior preserved and no production cutover.
</success_criteria>

<output>
After completion, create `131-02-SUMMARY.md` in this phase directory.
</output>

