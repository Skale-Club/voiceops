---
phase: 131-trusted-omnichannel-invocation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/action-engine.test.ts
  - tests/vapi-call-events.test.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "The existing Vapi tools route regression suite exercises the current maybeSingle-based tenant resolver and passes without changing production route behavior"
    - "Focused Action Engine dispatcher tests do not contact live Redis or another external service"
    - "The Vapi route still returns HTTP 200 and the original toolCallId on success and failure paths"
  artifacts:
    - path: "tests/action-engine.test.ts"
      provides: "Deterministic Vapi route and Action Engine baseline"
    - path: "tests/vapi-call-events.test.ts"
      provides: "Trusted assistant/number organization-resolution baseline"
  key_links:
    - from: "tests/action-engine.test.ts"
      to: "src/lib/vapi/end-of-call.ts"
      via: "mock query chain implements order, limit, and maybeSingle"
      pattern: "maybeSingle"
---

<objective>
Restore a trustworthy focused regression baseline before introducing omnichannel behavior. Update only test doubles and module isolation unless a production defect is proven. Preserve the current Vapi route output and Action Engine behavior byte-for-byte.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/omnichannel-agent-orchestration/phases/131-trusted-omnichannel-invocation-foundation/131-CONTEXT.md
@src/app/api/vapi/tools/route.ts
@src/lib/vapi/end-of-call.ts
@src/lib/action-engine/execute-action.ts
@tests/action-engine.test.ts
@tests/vapi-call-events.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair Vapi query-chain test doubles for the current resolver contract</name>
  <files>tests/action-engine.test.ts, tests/vapi-call-events.test.ts</files>
  <read_first>
    - src/lib/vapi/end-of-call.ts
    - src/app/api/vapi/tools/route.ts
    - tests/action-engine.test.ts
    - tests/vapi-call-events.test.ts
  </read_first>
  <action>
    In `tests/action-engine.test.ts`, extend `makeSingleChain` with chainable `order()` and `limit()` methods and a `maybeSingle()` terminal resolving the same canned result as `single()`. Keep `single()` because workflow resolution still uses it. Ensure `buildMockSupabase` can satisfy the assistant mapping lookup performed by `resolveOrgForCall` without falling into the outer `Service unavailable` catch. Update only assertions whose selected column now legitimately includes `entry_agent_id` after Plan 03; do not weaken HTTP status, result string, toolCallId, or deferred-log assertions. In `tests/vapi-call-events.test.ts`, verify its fake chains already support `maybeSingle`; add `entry_agent_id: null` to mapping fixtures only where the production select requires it. Do not mock `resolveOrgForCall` itself.
  </action>
  <verify>
    <automated>npx vitest run tests/action-engine.test.ts tests/vapi-call-events.test.ts</automated>
  </verify>
  <done>The two focused files run without stale query-chain failures and all Vapi route assertions keep HTTP 200 plus matching result IDs.</done>
  <acceptance_criteria>
    - `tests/action-engine.test.ts` contains `maybeSingle: vi.fn().mockResolvedValue(result)` or an equivalent shared terminal mock
    - `tests/action-engine.test.ts` contains chainable `order` and `limit` methods
    - `npx vitest run tests/action-engine.test.ts tests/vapi-call-events.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove external Redis dependence from the Action Engine unit baseline</name>
  <files>tests/action-engine.test.ts</files>
  <read_first>
    - src/lib/action-engine/execute-action.ts
    - src/lib/ghl/create-contact.ts
    - src/lib/rate-limit.ts
    - tests/action-engine.test.ts
  </read_first>
  <action>
    Re-run the focused suite after Task 1 and identify the import that opens Redis during the `executeAction('create_contact')` dispatcher test. Add a hoisted Vitest mock for that exact module or its external client before importing `execute-action`, returning deterministic in-memory/no-op behavior. Preserve the assertion that the mocked `createContact` function is called and its string result is returned. Do not add sleeps, raise test timeouts, make Redis failures pass conditionally, or change production rate-limit behavior.
  </action>
  <verify>
    <automated>npx vitest run tests/action-engine.test.ts --testTimeout=10000</automated>
  </verify>
  <done>The Action Engine test completes inside the 10-second test timeout with no Redis network log or retry.</done>
  <acceptance_criteria>
    - `npx vitest run tests/action-engine.test.ts --testTimeout=10000` exits 0
    - The test file contains no `setTimeout(` sleep and no per-test timeout greater than 10000
    - The create-contact dispatcher assertion still expects `Contact created. ID: cid_123`
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `npx vitest run tests/action-engine.test.ts tests/vapi-call-events.test.ts --testTimeout=10000`. Confirm no source file changed as part of this plan.
</verification>

<success_criteria>
The pre-change Vapi and Action Engine behavior has a deterministic, green regression baseline that can distinguish later implementation defects from stale mocks or external Redis failures.
</success_criteria>

<output>
After completion, create `131-01-SUMMARY.md` in this phase directory.
</output>

