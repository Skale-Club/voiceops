---
phase: 131-trusted-omnichannel-invocation-foundation
plan: 131-01
task: 1
total_tasks: 7
status: ready_to_execute
last_updated: 2026-09-03T19:11:56.189Z
workstream: omnichannel-agent-orchestration
---

<current_state>
The operator requested a shared specialist-agent architecture for voice and text and authorized execution. A dedicated v3.5 workstream has 32 requirements and a six-phase roadmap (131-136). Phase 131 now has three executable plans across two waves. The external checker produced no output and was interrupted; `131-PLAN-REVIEW.md` records a manual fallback pass. No production source code or database migration has been implemented yet.
</current_state>

<completed_work>

- Deep repository and live Cuts & Culture architecture analysis completed.
- Workstream `.planning/workstreams/omnichannel-agent-orchestration/` created and selected in `.planning/active-workstream`.
- `REQUIREMENTS.md` created with 32 active requirements and 32/32 roadmap traceability.
- `ROADMAP.md` created with Phases 131-136.
- `STATE.md` created with locked decisions and Phase 131 as current focus.
- `ARCHITECTURE-ANALYSIS.md` created with current paths, live tenant snapshot, critical gaps, target graph, constraints, files, and verification targets.
- `131-CONTEXT.md` created with Phase 131 boundary and locked implementation decisions.
- A phase researcher was attempted twice; the first attempt stalled and was interrupted, and the retry failed because the secondary-agent usage limit was reached. The operator explicitly said `continue`, so research was skipped using the GSD fallback.
- The operator then said `execute`, authorizing implementation after planning/verification gates.
</completed_work>

<remaining_work>

1. Execute Wave 1 plans `131-01` and `131-02`; create their summaries and atomic commits.
2. Execute dependent Wave 2 plan `131-03` only after Wave 1 tests pass.
3. Verify focused tests and `npm run build`; create `131-VERIFICATION.md` and only then mark Phase 131 complete.
4. Plan and execute Phases 132-136 sequentially. Do not activate Cuts & Culture or switch production Vapi routing before Phase 135 passes.
</remaining_work>

<decisions_made>

- Vapi owns telephony/STT/TTS/live conversation; Xphere owns internal agent orchestration and actions.
- Voice and text share agent definitions; channel overrides express differences.
- Explicit Vapi functions can directly call mapped specialists; ambiguous intents use an orchestrator.
- Voice normally permits at most one internal specialist model invocation before deterministic action execution.
- Direct tool permission must be separated from delegation permission in Phase 132.
- Every Xphere-owned generative call must use OpenRouter; embeddings are evaluated separately.
- Action Engine remains the unique provider execution boundary.
- `assistant_mappings.entry_agent_id` is nullable, additive, tenant-safe, and has no Phase 131 production backfill.
- Phase 131 creates foundation only and does not cut production Vapi traffic over.
- Cuts & Culture is the first isolated canary, never a universal seed/default.
</decisions_made>

<blockers>

- Secondary-agent usage quota blocked the Phase 131 researcher retry. Workaround accepted by operator: continue without RESEARCH.md using the detailed CONTEXT.md and architecture analysis.
- Detailed Phase 131 planner may encounter the same quota limit. If it does, create PLAN.md locally or resume after quota reset.
- Existing focused baseline has five known failures: four stale Vapi mocks missing `.maybeSingle()` and one Redis-dependent Action Engine timeout. Phase 131 must repair these before behavioral cutover.
</blockers>

<human_actions_pending>

- No immediate credential, deployment, or production action is required for Phase 131.
- Do not run `npx supabase db push` or enable any Vapi/tenant feature flag until implementation plans explicitly reach that gate and the operator approves production mutation if required.
- Phase 136 will require live voice/widget UAT and a carefully controlled real booking/replay canary.
</human_actions_pending>

<context>
The key architectural insight is that specialization must not add a model-call waterfall to voice. Build one trusted ingress boundary, then capability-edge authorization and explicit direct-specialist routing, then idempotent/timeout-safe Vapi integration. Preserve legacy behavior via null mapping and per-channel rollback throughout. All relevant evidence and exact files are in `ARCHITECTURE-ANALYSIS.md` and `131-CONTEXT.md`.
</context>

<next_action>
Start with Wave 1: execute `131-01-PLAN.md` and `131-02-PLAN.md`. Run their focused tests, create summaries, and proceed to `131-03-PLAN.md` only if both pass.
</next_action>
