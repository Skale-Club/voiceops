---
phase: 133-idempotent-action-and-vapi-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent-runtime/channel-policy.ts
  - src/lib/agent-runtime/guardrails.ts
  - src/lib/agent-runtime/invocation-gateway.ts
  - tests/agent-voice-latency-policy.test.ts
autonomous: true
requirements: [PERF-01]
---

<objective>
Give the voice channel a model-invocation ceiling expressed through the budget Phase 132
already shares across the invocation tree, so a normal lookup reaches deterministic tool
execution fast and exhaustion degrades to a lean recoverable result.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define channel latency policy as data</name>
  <files>src/lib/agent-runtime/channel-policy.ts, tests/agent-voice-latency-policy.test.ts</files>
  <action>Add a channel-keyed latency policy carrying a maximum number of internal specialist model invocations per turn and a wall-clock ceiling. Voice normally permits at most one internal specialist model invocation before deterministic tool execution; text keeps today's looser behavior. Keep the policy channel-neutral in shape and tenant-overridable in principle — do not hardcode Vapi, a tenant slug, or Cuts & Culture. Read defaults from one place so a later phase can source them per organization without touching call sites.</action>
  <verify>npx vitest run tests/agent-voice-latency-policy.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the ceiling on the shared budget and degrade cleanly</name>
  <files>src/lib/agent-runtime/guardrails.ts, src/lib/agent-runtime/invocation-gateway.ts, tests/agent-voice-latency-policy.test.ts</files>
  <action>Extend the Phase 132 tree-shared partner budget so it also counts internal specialist model invocations against the channel policy, rather than adding a second independent limiter. On exhaustion return a lean, recoverable outcome the caller can turn into a short channel response — never an exception, never a hang, and never a partial side effect. Deterministic tool execution that needs no further model call must still be allowed to complete after the model ceiling is reached.</action>
  <verify>npx vitest run tests/agent-voice-latency-policy.test.ts tests/agent-delegation.test.ts tests/agent-invocation-gateway.test.ts</verify>
</task>

</tasks>

<success_criteria>A voice turn cannot exceed its configured internal model-invocation ceiling; exhaustion yields a lean recoverable result rather than an error or a hang; text behavior is unchanged; no second limiter exists alongside the Phase 132 budget.</success_criteria>
