---
phase: 133-idempotent-action-and-vapi-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent-runtime/idempotency.ts
  - tests/idempotency-ingress-key.test.ts
autonomous: true
requirements: [SAFE-01, PERF-03]
---

<objective>
Make the existing idempotency key survive a channel retry and give an abandoned
side-effecting operation a traceable owner, without replacing the mechanism that
already works.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the key from trusted ingress identity</name>
  <files>src/lib/agent-runtime/idempotency.ts, tests/idempotency-ingress-key.test.ts</files>
  <action>Add an ingress-scoped derivation alongside the existing `deriveIdempotencyKey(invocationId, toolCallIndex)`, which stays as the fallback for paths with no ingress identity (widget, campaigns, cron). The new form takes a trusted channel identity — for Vapi, `call.id` plus `toolCall.id` — and produces the same key across retries of the same delivery. Keep the sha256 shape and the existing column contract. Accept the identity only as an explicit server-side argument; never read it from tool arguments or model output. Preserve the current key derivation byte-for-byte for existing callers so no in-flight key changes meaning.</action>
  <verify>npx vitest run tests/idempotency-ingress-key.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate replay from conflict, and record abandoned work</name>
  <files>src/lib/agent-runtime/idempotency.ts, tests/idempotency-ingress-key.test.ts</files>
  <action>Make `checkIdempotency` return a discriminated outcome: fresh, replay (carrying the original recorded result), or conflict when the key matches but `hashToolArgs()` does not. A conflict must be surfaced to the caller, never silently answered with the original result. Add a way to record that a side-effecting execution was abandoned mid-flight — a timeout or an abort — so the row states that ownership is unresolved rather than leaving the slot to look free to a later retry. An unresolved row must not be treated as a successful replay.</action>
  <verify>npx vitest run tests/idempotency-ingress-key.test.ts tests/agent-delegation.test.ts</verify>
</task>

</tasks>

<success_criteria>The same trusted ingress delivery derives one key across retries; a same-key/different-args request is reported as a conflict; an abandoned side-effecting execution leaves a row that a later retry cannot mistake for either a free slot or a completed result.</success_criteria>
