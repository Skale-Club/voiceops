---
phase: 133-idempotent-action-and-vapi-safety
plan: 03
type: execute
wave: 2
depends_on: [133-01, 133-02]
files_modified:
  - src/app/api/vapi/tools/route.ts
  - tests/vapi-tools-idempotency.test.ts
  - tests/vapi-tools-multicall.test.ts
  - tests/vapi-tools-http200-contract.test.ts
autonomous: true
requirements: [SAFE-02, OBS-03, PERF-02, PERF-03]
---

<objective>
Make the Vapi tool webhook execute side effects exactly once, answer every tool call in
a payload, own its timeouts, and keep its unconditional HTTP 200 contract.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard side-effecting executions with the ingress-scoped key</name>
  <files>src/app/api/vapi/tools/route.ts, tests/vapi-tools-idempotency.test.ts</files>
  <action>Wrap `executeAction()` with the 133-01 guard, keyed on the trusted `call.id` plus `toolCall.id` from the verified webhook payload. Apply it only where `requiresIdempotency()` says so — reads must not pay for the guard or be blocked by it. On replay return the originally recorded result instead of re-executing. On conflict return a distinct, channel-safe result rather than the original. Never derive the key from tool arguments or model output.</action>
  <verify>npx vitest run tests/vapi-tools-idempotency.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Answer every tool call in the payload</name>
  <files>src/app/api/vapi/tools/route.ts, tests/vapi-tools-multicall.test.ts</files>
  <action>Replace the `toolCallList[0]` truncation. Execute every supported call and return one result per call with matching tool-call ids, or reject an unsupported payload shape deterministically with a result for each call. No call may be silently dropped. Keep per-call failures isolated so one failing call cannot suppress the results of the others, and keep the response lean.</action>
  <verify>npx vitest run tests/vapi-tools-multicall.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Own timeouts and lock the HTTP 200 contract</name>
  <files>src/app/api/vapi/tools/route.ts, tests/vapi-tools-http200-contract.test.ts</files>
  <action>On an abort or timeout during a side-effecting action, record the abandoned-ownership state from 133-01 before returning the fallback message, so a later retry cannot treat the slot as free and reconciliation can tell whether the mutation landed. Add a regression suite asserting HTTP 200 with a lean body on every path: rejected secret, malformed JSON, schema-parse failure, unresolvable org, unconfigured tool, executor throw, timeout, and unexpected outer error. Assert the handler still declares the Node.js runtime, still defers logging through `after()`, and that any first-party target uses the canonical `https://xphere.app` origin. Do not restructure the handler beyond what these requirements need — it is latency-sensitive and currently correct on the 200 contract.</action>
  <verify>npx vitest run tests/vapi-tools-http200-contract.test.ts tests/vapi-tools-idempotency.test.ts tests/vapi-tools-multicall.test.ts</verify>
</task>

</tasks>

<success_criteria>A duplicated Vapi delivery mutates the provider once and replays the original result; every tool call in a multi-call payload gets a matching result id; a timed-out side effect leaves traceable ownership; every handled and error path still returns HTTP 200 with a lean payload on the Node.js runtime.</success_criteria>
