---
phase: 137-shared-specialist-mesh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent-runtime/resolve-specialist-route.ts
  - src/app/api/vapi/tools/route.ts
  - tests/vapi-specialist-dispatch.test.ts
autonomous: true
requirements: [MESH-02]
---

<objective>
Let a Vapi tool call reach an Xphere specialist instead of always going straight to the
Action Engine, without adding a second inference to a call that is already slow.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map an explicit Vapi function to its specialist</name>
  <files>src/lib/agent-runtime/resolve-specialist-route.ts, tests/vapi-specialist-dispatch.test.ts</files>
  <action>Resolve a trusted tool name to the specialist that owns that domain, for the resolved organization and channel, with no orchestrator model call. The mapping must be tenant-configured and channel-neutral - derive it from the agents' own granted workflows rather than hardcoding a table of names, so a tenant that names its tools differently still works. Unknown name, inactive agent, or no specialist owning that tool must all resolve to no route rather than to a guess.</action>
  <verify>npx vitest run tests/vapi-specialist-dispatch.test.ts tests/agent-specialist-routing.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch through the specialist only when it earns its latency</name>
  <files>src/app/api/vapi/tools/route.ts, tests/vapi-specialist-dispatch.test.ts</files>
  <action>Gate the new path behind the Phase 134 channel routing mode, so an organization on legacy behaves exactly as it does today and this merges as a no-op. In specialist mode, dispatch an explicit tool call to its specialist through the trusted gateway; when no specialist owns the tool, fall back to the current direct Action Engine path rather than failing. Measured fact from 137-CONTEXT.md: check_availability alone costs 6.5 to 8.3 seconds, so a second inference is not free - keep exactly one internal model call per voice tool call. Preserve every Phase 133 guarantee: HTTP 200 on every path, per-call isolation across a multi-call payload, the ingress-scoped idempotency guard, and abandoned-ownership recording on timeout.</action>
  <verify>npx vitest run tests/vapi-specialist-dispatch.test.ts tests/vapi-tools-http200-contract.test.ts tests/vapi-tools-idempotency.test.ts tests/vapi-tools-multicall.test.ts</verify>
</task>

</tasks>

<success_criteria>An explicit Vapi function reaches its specialist with one internal inference; an organization in legacy mode is byte-for-byte unchanged; an unmapped tool still executes; every Phase 133 guarantee holds.</success_criteria>
