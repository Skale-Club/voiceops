---
phase: 137-shared-specialist-mesh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/omnichannel-agent-orchestration/canary/cuts-and-culture.json
  - scripts/provision-canary-graph.ts
  - tests/canary-graph-shape.test.ts
autonomous: true
requirements: [MESH-01, MESH-03]
---

<objective>
Make the canary graph describe the tenant that actually exists, then provision it, so the
specialists stop being a document and become rows.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conform the graph to the tenant and to the script's contract</name>
  <files>.planning/workstreams/omnichannel-agent-orchestration/canary/cuts-and-culture.json, tests/canary-graph-shape.test.ts</files>
  <action>The graph was rewritten with correct prompts and correct tool names but in a shape the provisioning script cannot read. Conform it to CanaryGraph/CanaryAgentDef/CanaryWorkflowDef/CanaryEdgeDef as declared in scripts/provision-canary-graph.ts, keeping every system prompt and every real tool name exactly as written. Use the organization slug and the eight tool names from 137-CONTEXT.md. Keep the two specialist-to-specialist edges (booking to customer, booking to availability) - a star topology would not be the mesh this phase exists to build. Update the shape tests for the real tool names.</action>
  <verify>npx vitest run tests/canary-graph-shape.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the tenant's existing workflows instead of creating duplicates</name>
  <files>scripts/provision-canary-graph.ts, tests/canary-graph-shape.test.ts</files>
  <action>All eight workflows already exist for this organization. The script must bind to them by tool_name and never create a second row for a tool_name that is already there, because a duplicate would silently split the tool surface. Assert by test that a provisioning run against an organization that already has all eight workflows creates zero new workflow rows and still produces the correct grants.</action>
  <verify>npx vitest run tests/canary-graph-shape.test.ts</verify>
</task>

<task type="auto">
  <name>Task 3: Dry run, then provision</name>
  <files>none - execution only</files>
  <action>Run the script with no arguments, then with the organization id only, and read both outputs. Only if the dry run is correct, run it with the apply flag against organization 31502b7d-f4bd-4493-91f7-fc6f2738a09d. Then verify against the live database: six agents present, seven partner edges present, and write grants attached to the booking edge and to no other. Do not touch the existing generalist agent - it stays active as the demo fallback.</action>
  <verify>npx vitest run tests/canary-graph-shape.test.ts</verify>
</task>

</tasks>

<success_criteria>Six agents and seven edges exist for the tenant; only the booking edge carries write grants; no duplicate workflow rows were created; the existing generalist is untouched.</success_criteria>
