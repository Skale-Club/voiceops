---
phase: 138-tenant-safe-meta-prospect-audiences
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/meta/audience-members.ts
  - src/lib/meta/audience-diff.ts
  - src/lib/meta/custom-audiences.ts
  - tests/meta-audience-members.test.ts
  - tests/meta-audience-diff.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, COMP-01, COMP-03, SYNC-02, TEST-01]
decisions: [D-01, D-03, D-04, D-05, D-06, D-07, D-11, D-14]
must_haves:
  truths:
    - "Only entities selected by the configured source rule become candidates."
    - "Contacts and accounts project into one deterministic hashed member contract."
    - "Unsubscribe, DND, suppression, archive, deletion, and identifier changes generate deterministic exclusions/removals."
---

<objective>
Build and prove the pure eligibility, hashing, deduplication, and membership-diff core before wiring any database job or Meta write.
</objective>

<threat_model>
- Accidental whole-CRM targeting (HIGH): the projector requires an explicit source definition and defaults to `source_type = 'xcraper'` for the master audience.
- Suppressed-person inclusion (HIGH): exclusion reasons are centralized and tested across contact/account representations.
- Hash mismatch (MEDIUM): normalization is canonical and test vectors pin exact outputs.
</threat_model>

<tasks>
<task type="tdd">
  <name>RED - Specify the canonical audience member projector</name>
  <files>tests/meta-audience-members.test.ts</files>
  <read_first>
    - src/lib/meta/custom-audiences.ts
    - src/lib/meta/graph.ts
    - src/lib/contacts/zod-schemas.ts
    - src/lib/phone-numbers/normalize.ts
    - supabase/migrations/1247_prospect_rows_view.sql
    - src/app/api/v1/optout/route.ts
  </read_first>
  <action>Add failing table-driven tests for: Xcraper contact email/phone; Xcraper account email from `custom_fields.email` and account phone; unrelated CRM source exclusion; entity pairing with missing keys; normalized duplicate collapse; `dnd_enabled`; `engagement_status = unsubscribed`; email suppression; archived duplicate; deleted/missing identifiers; verified/unknown email visibility; and raw PII absent from projected persisted/loggable output.</action>
  <acceptance_criteria>
    - Tests contain fixtures for both `contact` and `account` entities.
    - Tests assert unrelated non-Xcraper contacts are excluded from `xcraper_master`.
    - Tests assert all requested eligible identifiers remain included regardless of `email_status`, while status is retained as safe metadata.
    - RED run fails because the projector does not exist yet.
  </acceptance_criteria>
  <verify>`npx vitest run tests/meta-audience-members.test.ts` fails for missing implementation before GREEN.</verify>
</task>

<task type="tdd">
  <name>GREEN/REFACTOR - Implement projector and deterministic membership diff</name>
  <files>src/lib/meta/audience-members.ts, src/lib/meta/audience-diff.ts, src/lib/meta/custom-audiences.ts, tests/meta-audience-diff.test.ts</files>
  <read_first>
    - tests/meta-audience-members.test.ts
    - src/lib/meta/custom-audiences.ts
    - src/lib/meta/graph.ts
  </read_first>
  <action>Implement pure typed functions that normalize and hash both entity kinds, return explicit exclusion reasons, pair hashes per entity, deduplicate deterministically, and diff current members against last successful memberships. A changed email/phone must return REMOVE using old hashes and ADD using new hashes; unchanged fingerprints return no operation. Reuse `sha256Hex`/phone normalization but prevent raw identifiers from entering returned loggable metadata. Add exact hash vectors and diff tests.</action>
  <acceptance_criteria>
    - One eligible account with email+phone yields one member containing two hashes.
    - Changed email or phone yields one old REMOVE entry and one new ADD entry.
    - Unchanged input yields empty ADD and REMOVE arrays.
    - Duplicate normalized identifiers are submitted once according to documented entity-key ordering.
    - Projected persistence/log structures contain no raw identifier values.
  </acceptance_criteria>
  <verify>`npx vitest run tests/meta-audience-members.test.ts tests/meta-audience-diff.test.ts tests/meta-custom-audiences.test.ts` exits 0.</verify>
</task>
</tasks>

<verification>
The pure core proves the exact population and diff semantics independently of Supabase or Meta availability.
</verification>

<success_criteria>
The code can derive safe, deterministic audience membership for both Xcraper accounts and contacts and calculate every required add/remove transition.
</success_criteria>
