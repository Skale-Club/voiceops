---
phase: 138-tenant-safe-meta-prospect-audiences
plan: "05"
type: tdd
wave: 3
depends_on: ["138-01", "138-02", "138-03", "138-04"]
files_modified:
  - src/lib/meta/audience-reconcile.ts
  - scripts/meta-audience-sync.ts
  - .github/workflows/meta-audience-sync.yml
  - tests/meta-audience-reconcile.test.ts
  - tests/meta-audience-job.test.ts
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, OPS-02, TEST-01, TEST-02]
decisions: [D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "The worker reconciles ledger state rather than scanning all CRM contacts by an unsafe watermark."
    - "Partial failure never commits new successful membership or advances progress."
    - "Missing credentials/configuration is visible as misconfigured/failed, not green success."
---

<objective>
Implement the locked, retryable audience reconciliation engine and production scheduler.
</objective>

<threat_model>
- Partial-sync data loss (HIGH): commit ledger only after all remote batches succeed.
- Duplicate concurrent mutation (HIGH): per-audience lock/claim and stale-claim recovery.
- False operational success (MEDIUM): missing preconditions create actionable non-success state and workflow failure/alert.
</threat_model>

<tasks>
<task type="tdd">
  <name>RED/GREEN - Build transactional reconciliation</name>
  <files>src/lib/meta/audience-reconcile.ts, tests/meta-audience-reconcile.test.ts</files>
  <read_first>
    - src/lib/meta/audience-members.ts
    - src/lib/meta/audience-diff.ts
    - src/lib/meta/audience-provider.ts
    - src/lib/meta/custom-audiences.ts
    - scripts/meta-audience-sync.ts
    - supabase/migrations/1271_meta_prospect_audiences.sql
  </read_first>
  <action>Write tests first, then implement a per-audience reconciliation service that claims a run, validates terms/config/tenant credentials, obtains or idempotently creates the remote audience, streams the explicit source scope, projects current membership, diffs against the last successful ledger, sends REMOVE before ADD in bounded batches, and atomically commits the new ledger plus successful run state only after all batches succeed. Preserve prior state on errors, map safe error codes, recover stale claims, and make unchanged reruns zero-diff.</action>
  <acceptance_criteria>
    - First run creates/uses one remote audience and submits the projected members.
    - Changed identifier submits old REMOVE before new ADD.
    - Simulated failure in any batch leaves the previous membership ledger unchanged.
    - Two concurrent calls yield one active owner; the other safely skips/queues.
    - Repeating an unchanged successful run yields sent=0 and removed=0.
    - Run history contains safe counts/correlation/error code but no raw PII/token.
  </acceptance_criteria>
  <verify>`npx vitest run tests/meta-audience-reconcile.test.ts` exits 0.</verify>
</task>

<task type="execute">
  <name>Replace the prototype cron script and false-green workflow</name>
  <files>scripts/meta-audience-sync.ts, .github/workflows/meta-audience-sync.yml, tests/meta-audience-job.test.ts</files>
  <read_first>
    - scripts/meta-audience-sync.ts
    - .github/workflows/meta-audience-sync.yml
    - src/lib/meta/audience-reconcile.ts
  </read_first>
  <action>Make the script enumerate enabled/dirty audience configs and invoke the reconciliation service with optional `--org`, `--audience`, and `--dry-run`. Remove the contacts-only queries. Configure the workflow with Supabase service credentials required by the tenant provider path; do not require a global Meta token in tenant mode. Missing required configuration must fail with a clear job summary, while no dirty audiences is a legitimate success. Preserve hourly schedule and manual dry-run inputs.</action>
  <acceptance_criteria>
    - Script contains no direct whole-table contacts-only audience selection.
    - `--org` and `--audience` constrain every processed config explicitly.
    - Dry run performs zero Graph mutations and zero successful-ledger commits.
    - Missing Supabase/runtime configuration exits non-zero with a named missing key.
    - No-work exits zero and reports `0 audiences due`; missing credentials do not masquerade as no-work.
  </acceptance_criteria>
  <verify>`npx vitest run tests/meta-audience-job.test.ts tests/meta-audience-reconcile.test.ts` exits 0 and workflow YAML parses.</verify>
</task>
</tasks>

<verification>
Failure-injection, concurrency, dry-run, and scheduler tests pass; no false-green missing-config path remains.
</verification>

<success_criteria>
Audience membership is reconciled deterministically and recoverably through the same service for event-driven and hourly execution.
</success_criteria>
