---
phase: 138-tenant-safe-meta-prospect-audiences
plan: "04"
type: execute
wave: 2
depends_on: ["138-01", "138-02"]
files_modified:
  - src/app/api/v1/prospects/route.ts
  - src/app/api/v1/optout/route.ts
  - src/lib/meta/audience-dirty.ts
  - tests/prospect-company-enrichment.test.ts
  - tests/prospect-optout-meta.test.ts
autonomous: true
requirements: [DATA-03, SYNC-01, COMP-01, TEST-02]
decisions: [D-10, D-14]
must_haves:
  truths:
    - "Re-import enriches existing Xcraper accounts instead of dropping new email/metadata."
    - "Prospect changes and opt-outs durably request audience reconciliation."
    - "Account email-only opt-out matches custom_fields.email and produces removal eligibility."
---

<objective>
Close the ingestion and suppression gaps so source data changes reliably reach the audience reconciliation pipeline.
</objective>

<threat_model>
- Null clobber/data corruption (HIGH): merge only present, non-empty enrichment keys.
- Opt-out not propagated (HIGH): mark affected audiences dirty after committed opt-out/suppression changes.
- Unauthorized dirty marking (MEDIUM): derive org only from the authenticated API key/request context.
</threat_model>

<tasks>
<task type="execute">
  <name>Merge Xcraper company enrichment on idempotent re-import</name>
  <files>src/app/api/v1/prospects/route.ts, tests/prospect-company-enrichment.test.ts</files>
  <read_first>
    - src/app/api/v1/prospects/route.ts
    - C:/Users/Vanildo/Dev/xcraper/backend/src/services/xphere.ts
    - src/types/database.ts
  </read_first>
  <action>For an existing prospect-stage account, merge present `custom_fields` keys (including email, website, address, category, rating, review count and Maps URL), merge source payload/socials, update website/domain/phone, and preserve existing non-null values when the new payload omits or sends null. Keep source-ID/domain/name dedup semantics and never pull converted CRM accounts back to prospect.</action>
  <acceptance_criteria>
    - Re-import with a newly discovered email updates `accounts.custom_fields.email`.
    - Re-import with null email does not erase an existing email.
    - Source payload/social fields merge without erasing unrelated existing keys.
    - A non-prospect existing account remains skipped and unchanged.
  </acceptance_criteria>
  <verify>`npx vitest run tests/prospect-company-enrichment.test.ts` exits 0.</verify>
</task>

<task type="execute">
  <name>Unify opt-out eligibility and dirty marking</name>
  <files>src/app/api/v1/optout/route.ts, src/lib/meta/audience-dirty.ts, tests/prospect-optout-meta.test.ts</files>
  <read_first>
    - src/app/api/v1/optout/route.ts
    - src/lib/meta/audience-members.ts
    - supabase/migrations/1271_meta_prospect_audiences.sql
  </read_first>
  <action>Add a small organization-scoped dirty-marker service and call it after successful Xcraper ingest updates/inserts and after opt-out/suppression changes. Update opt-out matching to find account email in `custom_fields.email` as well as phone. Make contact/account exclusion semantics consistent by treating DND, unsubscribed engagement, and suppression as removal inputs; do not falsely set channel-specific consent fields without an explicit channel request.</action>
  <acceptance_criteria>
    - Successful Xcraper create/update marks only matching enabled audience scopes in the same organization dirty.
    - Email-only account opt-out finds an account whose email is stored in `custom_fields.email`.
    - Contact and account unsubscribe lead to projector exclusion and a dirty audience.
    - Failed ingestion/opt-out writes do not mark an audience as successfully reconciled.
  </acceptance_criteria>
  <verify>`npx vitest run tests/prospect-company-enrichment.test.ts tests/prospect-optout-meta.test.ts` exits 0.</verify>
</task>
</tasks>

<verification>
Ingestion and opt-out tests prove that every relevant source change becomes durable reconciliation work without cross-tenant effects.
</verification>

<success_criteria>
Xcraper enrichment and consent changes can no longer be silently lost between source ingestion and audience membership.
</success_criteria>
