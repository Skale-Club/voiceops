---
phase: 138-tenant-safe-meta-prospect-audiences
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/meta/audience-provider.ts
  - src/lib/meta/graph.ts
  - src/lib/ads/meta-oauth.ts
  - tests/meta-audience-provider.test.ts
  - tests/meta-custom-audiences.test.ts
autonomous: true
requirements: [TEN-01, TEN-02, OPS-01, TEST-02]
decisions: [D-08, D-09]
must_haves:
  truths:
    - "Audience writes use the explicit Meta connection owned by the audience organization."
    - "Expired, inactive, inaccessible, or mismatched credentials fail before any Graph mutation."
    - "The Custom Audience transport uses a currently supported API contract."
---

<objective>
Replace the implicit global-token dependency with a fail-closed organization OAuth provider and current Meta API contract.
</objective>

<threat_model>
- Cross-tenant credential use (CRITICAL): connection lookup requires both connection ID and audience `org_id`.
- Token disclosure (HIGH): decrypt only in memory and redact all errors/logs.
- Stale API contract (MEDIUM): pin and test one centralized supported Graph version.
</threat_model>

<tasks>
<task type="execute">
  <name>Implement OrgOAuthProvider with strict ownership and expiry checks</name>
  <files>src/lib/meta/audience-provider.ts, tests/meta-audience-provider.test.ts</files>
  <read_first>
    - src/lib/meta/audience-provider.ts
    - src/app/api/ads/meta/callback/route.ts
    - src/app/(dashboard)/ads/capi/actions.ts
    - src/lib/crypto.ts
    - src/types/database.ts
  </read_first>
  <action>Add an organization OAuth provider that resolves one explicit `ads_connections` row by `id`, `org_id`, `platform = 'meta'`, selected ad-account ID, and `status = 'active'`; rejects expired/missing tokens; decrypts using the existing crypto helper; and returns a typed safe error code. Retain `AgencySystemUserProvider` only as an explicitly selected fallback mode. Never select the first available account or fall through to another organization.</action>
  <acceptance_criteria>
    - Correct org/connection/ad-account returns a token in memory.
    - Wrong org, inactive status, wrong ad account, expired token, decrypt failure, and missing row each reject before Graph fetch.
    - Test output and error strings never contain the fixture token.
    - No code path silently uses `META_SYSTEM_USER_TOKEN` when tenant mode is configured.
  </acceptance_criteria>
  <verify>`npx vitest run tests/meta-audience-provider.test.ts` exits 0.</verify>
</task>

<task type="execute">
  <name>Validate and pin the current Meta Custom Audience API contract</name>
  <files>src/lib/ads/meta-oauth.ts, src/lib/meta/graph.ts, src/lib/meta/custom-audiences.ts, tests/meta-custom-audiences.test.ts</files>
  <read_first>
    - src/lib/ads/meta-oauth.ts
    - src/lib/meta/graph.ts
    - src/lib/meta/custom-audiences.ts
    - tests/meta-custom-audiences.test.ts
  </read_first>
  <action>Check current official Meta Marketing API documentation at execution time, update the single `META_ADS_GRAPH_VERSION` constant if v20.0 is no longer supported, and align create/status/ADD/REMOVE payloads with the supported Customer List schema. Add tests for exact HTTP method, path, schema, local hashing, error mapping, and token redaction. Record the verified version and documentation date in a source comment or operations note.</action>
  <acceptance_criteria>
    - One centralized version constant drives OAuth, Ads, and Custom Audience Graph requests.
    - Tests assert POST for ADD, DELETE for REMOVE, and `EMAIL_SHA256`/`PHONE_SHA256` payload schema.
    - Graph errors expose safe code/message but never the access token or request hashes.
    - The verified API version/date is recorded next to the constant or in the deployment evidence.
  </acceptance_criteria>
  <verify>`npx vitest run tests/meta-custom-audiences.test.ts tests/meta-audience-provider.test.ts` exits 0.</verify>
</task>
</tasks>

<verification>
Tenant credential and transport tests pass without network writes or secret leakage.
</verification>

<success_criteria>
Every audience request has an explicit, valid, tenant-owned Meta connection and a verified supported API contract.
</success_criteria>
