# Continue Here

**Workstream:** `omnichannel-agent-orchestration` (always pass `--ws omnichannel-agent-orchestration`)
**Milestone:** v3.5 — 3 of 6 phases complete (50%)

## Done

- **Phase 131** Trusted Omnichannel Invocation Foundation — complete, verified.
- **Phase 132** Authorized Specialist Orchestration — complete, verified.
  Commits `599118e9`, `60690309`, `07baabcf`, `5351f00c`, plus docs `cac188e3`, `5dc371f9`.
  See `phases/132-authorized-specialist-orchestration/132-VERIFICATION.md`.
- **Phase 133** Idempotent Action and Vapi Safety — complete, verified.
  Commits `7b399031`, `e007da1c`, `c31368fb`, `3cc836aa`, `b6ad9f63`, `519ce698`.
  See `phases/133-idempotent-action-and-vapi-safety/133-VERIFICATION.md`.

## Next action

Phase 134 — **Traceability and Reversible Routing** (OBS-01, OBS-02, ROLL-02) — has no
context document and no plans.

1. Read `ROADMAP.md` (Phase 134 section) and the 8 requirements still marked Pending in `REQUIREMENTS.md`.
2. **Verify every gap against current source before writing the context.** The
   planning-time inventories in this workstream have been wrong twice — the Phase 132
   provider list named two non-violations and missed two real ones, and Phase 133's
   own plan assumed a gap the code had already closed.
3. Write `134-CONTEXT.md`, then generate and review plans.
4. Then: `$gsd-execute-phase 134 --ws omnichannel-agent-orchestration`

## Read before writing code

- `phases/132-authorized-specialist-orchestration/132-KNOWN-LIMITATIONS.md` — scoped
  knowledge retrieval under-retrieves by design; legacy tools fail closed through edges.
- `phases/132-authorized-specialist-orchestration/132-PROVIDER-DRIFT-INVENTORY.md` —
  the authoritative generative-provider list; the planning-time one was wrong.

## Environment traps

- The full test suite fails **30-32 files / 52-53 tests** for reasons unrelated to this
  workstream. The set is not stable: members beyond the core 30 shift between runs and
  pass in isolation (Postgres `deadlock detected` in `calendar-rls`, a vitest SSR
  module-cycle in `demo-readonly`). Measure against a fresh run and check any newcomer
  in isolation before calling it either a regression or noise — a delegated agent
  misfiled a real regression as baseline noise once already.
- Production build needs `NODE_OPTIONS=--max-old-space-size=8192`.
- **Never junction `node_modules` into a git worktree.** `git worktree remove --force`
  follows the junction and deletes the real directory (it happened; recovered with `npm ci`).

## Production boundary — still held

Migrations **1290 and 1291 are authored but unapplied**; Phase 133 added none.
`/api/vapi/tools` is **not cut over** — it was hardened in place and still runs the
legacy direct-`executeAction` path, not the specialist graph. No Vapi assistant bound,
no tenant agent modified, no live booking executed. Those gates belong to Phases 135-136.
