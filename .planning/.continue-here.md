# Continue Here

**Workstream:** `omnichannel-agent-orchestration` (always pass `--ws omnichannel-agent-orchestration`)
**Milestone:** v3.5 — all six phases built. 31 of 32 requirements done.

## Nothing autonomous remains

Every remaining step needs a person. Start here:

**`docs/agents/canary-activation-runbook.md`** — six ordered steps, each with a
precondition, an exact action, an observable signal, and an abort step. None performed.

## Migrations are applied

1290-1295 were applied on 2026-09-04 via `npx supabase db push`. The cross-organization leak
in `get_org_member_profiles` is closed, the suite is green, and it is a gate member again.

**One thing that came out of it needs a decision:** production had drifted from this repo —
that function had been changed in the database with no migration to match, and it only
surfaced because 1295 collided with it. One function was reconciled. Nothing else was
audited, and other objects may have drifted the same way. See `FINDINGS-OUTSIDE-SCOPE.md`
item 3.

## Read before touching anything

- `MILESTONE-SUMMARY.md` — what shipped, and the distinction between what is on live paths
  and what is beside them.
- `FINDINGS-OUTSIDE-SCOPE.md` — the security leak and the 24 unclassified write action types.
- Each phase's `13X-VERIFICATION.md`.

## The thing most likely to be misread

`invokeAgent` has **no production callers**. The widget calls `runAgent` directly and
`/api/vapi/tools` calls `executeAction` directly. So the gateway, specialist routing and the
channel routing switch are built and tested but not on any request path — flipping a routing
row today changes nothing. Runbook step 5.0 is the human code change that wires it.

What phases 132-134 put *inside* `runAgent` and the Vapi route is live today.

## Environment

- Full suite fails ~30 files for live-database and module-resolution reasons. One of those
  turned out to be a real security leak, so audit newcomers rather than assuming noise.
- `npm run release-gate` is the deterministic gate: exit 0 at HEAD.
- Build needs `NODE_OPTIONS=--max-old-space-size=8192`. A local Redis on `localhost:6379`
  being down makes static generation slow and flaky, but it passes on retry.
- Never junction `node_modules` into a git worktree.
