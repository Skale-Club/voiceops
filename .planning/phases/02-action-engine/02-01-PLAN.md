---
phase: 02-action-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/crypto.test.ts
  - tests/ghl-executor.test.ts
  - tests/action-engine.test.ts
  - tests/integrations.test.ts
autonomous: true
requirements: [ACTN-01, ACTN-02, ACTN-03, ACTN-04, ACTN-05, ACTN-06, ACTN-07, ACTN-08, ACTN-09, ACTN-10, ACTN-11, ACTN-12]

must_haves:
  truths:
    - "npx vitest run exits 0 after Wave 0 — all test files parse and stubs pass/skip"
    - "Each test file contains named describe blocks matching the requirement IDs it covers"
    - "No test file imports a module that does not yet exist (stubs use it.todo, not live imports)"
  artifacts:
    - path: "tests/crypto.test.ts"
      provides: "AES-256-GCM round-trip and masking stubs"
      contains: "ACTN-04"
    - path: "tests/ghl-executor.test.ts"
      provides: "GHL executor stubs for all three action types"
      contains: "ACTN-09"
    - path: "tests/action-engine.test.ts"
      provides: "Org resolution, tool routing, fallback, 500ms budget stubs"
      contains: "ACTN-01"
    - path: "tests/integrations.test.ts"
      provides: "Integration CRUD + test connection stubs"
      contains: "ACTN-03"
  key_links:
    - from: "tests/crypto.test.ts"
      to: "src/lib/crypto.ts"
      via: "it.todo stubs — file not imported yet"
      pattern: "it\\.todo"
    - from: "tests/action-engine.test.ts"
      to: "src/lib/action-engine/resolve-org.ts"
      via: "it.todo stubs"
      pattern: "it\\.todo"
---

<objective>
Create Wave 0 test stubs for Phase 2. Four new test files cover all ACTN requirement IDs with `it.todo` stubs and named describe blocks. No implementation code is imported — stubs allow the test suite to exit 0 immediately so every subsequent plan has a continuously passing test harness.

Purpose: Nyquist compliance — every task in Waves 1-3 has a backing test file that will become meaningful as implementation lands.
Output: 4 test files, all exiting clean under `npx vitest run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-action-engine/02-VALIDATION.md
</context>

<interfaces>
<!-- Existing test file pattern from Phase 1 — replicate this structure exactly -->

From tests/organizations.test.ts:
```typescript
import { describe, it, expect } from 'vitest'

const hasSupabase = !!process.env.NEXT_PUBLIC_SUPABASE_URL

describe('TEN-01: Organization CRUD', () => {
  it(hasSupabase ? 'can create...' : 'SKIP: no Supabase config', async () => {
    if (!hasSupabase) return
    expect(true).toBe(true)
  })
  it.todo('...')
})
```

Key rule: `it.todo()` stubs have NO implementation body — they are zero-argument `it.todo('description')` calls.
Vitest exits 0 when all tests are either passing or todo.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create crypto.test.ts — ACTN-04 stubs</name>
  <files>tests/crypto.test.ts</files>
  <read_first>
    - tests/organizations.test.ts (existing stub pattern to replicate)
    - .planning/phases/02-action-engine/02-VALIDATION.md (ACTN-04 coverage requirements)
  </read_first>
  <action>
Create `tests/crypto.test.ts` with `it.todo` stubs covering ACTN-04 (encryption at rest, masking in UI).

Exact content to write:

```typescript
import { describe, it } from 'vitest'

describe('ACTN-04: Credential encryption at rest', () => {
  it.todo('encrypt() produces iv:ciphertext format (base64:base64) — not plaintext')
  it.todo('decrypt(encrypt(plaintext)) returns original plaintext — round-trip')
  it.todo('two encrypt() calls on the same plaintext produce different ciphertext (IV randomness)')
  it.todo('decrypt() with wrong key throws — does not return garbage plaintext')
  it.todo('maskApiKey() returns last-4 characters preceded by asterisks — never full key')
})
```

No imports of `src/lib/crypto.ts` yet — that file does not exist. All stubs are `it.todo`.
  </action>
  <verify>
    <automated>npx vitest run tests/crypto.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `tests/crypto.test.ts` exists
    - File contains `describe('ACTN-04` (exact string match)
    - File contains exactly 5 `it.todo(` calls
    - `npx vitest run tests/crypto.test.ts` exits 0 with 5 todos reported
    - No import of `src/lib/crypto` present in the file
  </acceptance_criteria>
  <done>crypto.test.ts exits clean with 5 todos, no import errors</done>
</task>

<task type="auto">
  <name>Task 2: Create ghl-executor.test.ts — ACTN-09 stubs</name>
  <files>tests/ghl-executor.test.ts</files>
  <read_first>
    - tests/organizations.test.ts (stub pattern)
    - .planning/phases/02-action-engine/02-RESEARCH.md (GHL API patterns — Pattern 4)
  </read_first>
  <action>
Create `tests/ghl-executor.test.ts` with `it.todo` stubs covering ACTN-09 (three GHL action executors).

Exact content to write:

```typescript
import { describe, it } from 'vitest'

describe('ACTN-09: GHL createContact executor', () => {
  it.todo('sends POST to https://services.leadconnectorhq.com/contacts/ with Bearer token and Version header')
  it.todo('returns success string containing GHL contact ID on 201 response')
  it.todo('throws on non-2xx GHL response — caller handles fallback')
  it.todo('AbortController cancels request after 400ms timeout')
})

describe('ACTN-09: GHL getAvailability executor', () => {
  it.todo('sends GET to /calendars/:calendarId/free-slots with startDate and endDate query params')
  it.todo('returns formatted availability string (single line, no newlines)')
  it.todo('throws on non-2xx GHL response')
})

describe('ACTN-09: GHL createAppointment executor', () => {
  it.todo('sends POST to /calendars/events/appointments with calendarId, contactId, startTime, endTime')
  it.todo('returns success string containing appointment ID on 200 response')
  it.todo('throws on non-2xx GHL response')
})
```

No imports of `src/lib/ghl/*` yet — those files do not exist.
  </action>
  <verify>
    <automated>npx vitest run tests/ghl-executor.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `tests/ghl-executor.test.ts` exists
    - File contains 3 describe blocks with `ACTN-09` in each name
    - File contains exactly 10 `it.todo(` calls
    - `npx vitest run tests/ghl-executor.test.ts` exits 0
    - No import of `src/lib/ghl` present in the file
  </acceptance_criteria>
  <done>ghl-executor.test.ts exits clean with 10 todos, no import errors</done>
</task>

<task type="auto">
  <name>Task 3: Create action-engine.test.ts and integrations.test.ts — remaining stubs</name>
  <files>tests/action-engine.test.ts, tests/integrations.test.ts</files>
  <read_first>
    - tests/organizations.test.ts (stub pattern)
    - .planning/phases/02-action-engine/02-VALIDATION.md (coverage map for ENGINE-01, ROUTE-01, UI-INT)
  </read_first>
  <action>
Create two files.

**File 1: `tests/action-engine.test.ts`** — covers ACTN-01, ACTN-02, ACTN-11, ACTN-12:

```typescript
import { describe, it } from 'vitest'

describe('ACTN-01: Org resolution by assistant ID', () => {
  it.todo('resolveOrg(assistantId) returns organization_id for a known active assistant mapping')
  it.todo('resolveOrg(assistantId) returns null for unknown assistant ID')
  it.todo('resolveOrg(assistantId) returns null for inactive assistant mapping (is_active=false)')
})

describe('ACTN-02: Tool config routing', () => {
  it.todo('resolveTool(orgId, toolName) returns tool_config row with integration for a matching active config')
  it.todo('resolveTool(orgId, toolName) returns null for unknown tool name in that org')
  it.todo('resolveTool(orgId, toolName) returns null for inactive tool config (is_active=false)')
})

describe('ACTN-11: Fallback message on failure', () => {
  it.todo('POST /api/vapi/tools returns HTTP 200 with fallback_message when GHL executor throws')
  it.todo('POST /api/vapi/tools returns HTTP 200 with "Service unavailable." for unknown assistant ID')
  it.todo('POST /api/vapi/tools never returns HTTP non-200 — catches all errors in outer try/catch')
})

describe('ACTN-12: 500ms response budget', () => {
  it.todo('action_logs insert happens via after() — not awaited before Response.json() is returned')
  it.todo('GHL fetch uses AbortController with 400ms timeout signal')
})
```

**File 2: `tests/integrations.test.ts`** — covers ACTN-03, ACTN-05, ACTN-06, ACTN-07, ACTN-08:

```typescript
import { describe, it } from 'vitest'

describe('ACTN-03: Integration credential storage', () => {
  it.todo('createIntegration server action stores encrypted_api_key as iv:ciphertext — not plaintext')
  it.todo('getIntegrations server action returns integrations without encrypted_api_key field')
  it.todo('getIntegrations server action returns masked_api_key (last 4 chars) for display')
})

describe('ACTN-05: Test connection', () => {
  it.todo('testConnection server action calls GHL GET /contacts/ with decrypted credentials')
  it.todo('testConnection returns { success: true } on 200 response from GHL')
  it.todo('testConnection returns { success: false, error: string } on GHL error')
})

describe('ACTN-06 + ACTN-07: Tool configuration', () => {
  it.todo('createToolConfig server action writes tool_name, action_type, integration_id, fallback_message')
  it.todo('createToolConfig enforces unique (organization_id, tool_name) constraint — duplicate returns error')
  it.todo('updateToolConfig can change action_type and integration_id')
})

describe('ACTN-08: Fallback message per tool', () => {
  it.todo('tool_configs.fallback_message is required — createToolConfig rejects empty fallback_message')
  it.todo('fallback_message is returned verbatim to Vapi when the tool execution fails')
})
```
  </action>
  <verify>
    <automated>npx vitest run tests/action-engine.test.ts tests/integrations.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `tests/action-engine.test.ts` exists with 4 describe blocks containing ACTN-01, ACTN-02, ACTN-11, ACTN-12
    - `tests/integrations.test.ts` exists with 4 describe blocks containing ACTN-03, ACTN-05, ACTN-06, ACTN-08
    - `npx vitest run tests/action-engine.test.ts tests/integrations.test.ts` exits 0
    - Combined todo count across both files: 17 todos
    - Neither file imports any `src/lib/` module
  </acceptance_criteria>
  <done>Both files exit clean, full suite `npx vitest run` still exits 0</done>
</task>

</tasks>

<verification>
Run full test suite after all 3 tasks:

```bash
npx vitest run --reporter=verbose
```

Expected: all existing tests pass/skip as before, 4 new test files contribute todos only, exit code 0.

Grep check: `grep -r "it\.todo" tests/crypto.test.ts tests/ghl-executor.test.ts tests/action-engine.test.ts tests/integrations.test.ts | wc -l` should return 32 (5 + 10 + 12 + 5 todos).
</verification>

<success_criteria>
- 4 test files exist: tests/crypto.test.ts, tests/ghl-executor.test.ts, tests/action-engine.test.ts, tests/integrations.test.ts
- `npx vitest run` exits 0 with no red failures
- All stubs are `it.todo` — no live module imports from src/lib that don't yet exist
- VALIDATION.md Wave 0 checklist items can be ticked: all 4 files exist
</success_criteria>

<output>
After completion, create `.planning/phases/02-action-engine/02-01-SUMMARY.md`
</output>
